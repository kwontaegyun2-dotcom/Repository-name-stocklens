# StockLens 인계 문서 (HANDOFF)

> 이 문서만 읽고 바로 이어서 개발할 수 있도록 작성됨.
> 최종 갱신: 2026-07-24 · 캐시버전 **v17** · 백테스트·스크리너 제거, 밸류에이션 분석(valuation) 추가

---

## 1. 프로젝트 개요

**StockLens** — 국내(코스피/코스닥) + 미국 주식 종합 분석 웹앱.
종목을 6개 부문으로 점수화(S~F 등급)하고, 목표주가·매수/매도 타점·차트(5년)·고급 차트분석·밸류에이션 분석·뉴스를 제공.

| 항목 | 값 |
|---|---|
| 로컬 경로 | `C:\Users\권태균\stocklens` |
| 라이브 URL | https://stocklens-mpr6.onrender.com |
| GitHub | https://github.com/kwontaegyun2-dotcom/Repository-name-stocklens (main 브랜치) |
| 배포 | Render.com, Docker, 싱가포르 리전, **무료 플랜** (push → 자동 재배포) |
| Render serviceId | `srv-d968qp77f7vs73d5hd80` |
| 스택 | FastAPI + 바닐라 JS (프레임워크·빌드 없음), 외부 CDN: lightweight-charts, Pretendard |

> ⚠️ **별개 프로젝트 주의**: `C:\Users\권태균\stock-analyzer` 는 예전 Streamlit 분석기.
> 이 프로젝트와 무관하니 건드리지 말 것.

---

## 2. 완료된 부분 (파일별)

### 백엔드

| 파일 | 역할 | 상태 |
|---|---|---|
| `main.py` | FastAPI 엔트리. 전 엔드포인트, 공개모드 게이팅, 레이트리밋 | ✅ 완료 |
| `app/naver.py` | 네이버 비공식 API 클라이언트. **국내/미국 자동 라우팅** | ✅ 완료 |
| `app/analysis.py` | 분석 엔진 (기술적·펀더멘털·감성·점수화) | ✅ 완료 |
| `app/ranking.py` | 국내 132 / 미국 147종목 백그라운드 채점·캐싱 | ✅ 완료 |
| `app/valuation.py` | 밸류에이션 6항목(역사적PER·PEG·동종업계·EV/EBITDA·목표주가·실적방향) | ✅ 완료·배포 |
| ~~`app/screener.py`~~ | 스크리너 — **2026-07-24 사용자 요청으로 삭제** | 🗑 제거됨 |
| `app/dart.py` | DART 주요계정 → 영업이익 YoY·흑자전환 | ⚠️ **미사용** (유일한 소비처였던 screener 삭제됨) |
| `app/chart_pro.py` | 고급 차트 분석 10기법(스테이지·RS·추세템플릿·VCP·OBV 등) | ✅ 완료·배포 |
| `app/kis.py` | 한국투자증권 실시간 시세 (로컬 전용) | ✅ 완료 |
| `app/ai.py` | Claude AI 심층 리포트 (선택, 키 필요) | ✅ 완료 (공개모드 비활성) |

**엔드포인트 목록**
```
GET  /                      메인 HTML
GET  /api/search?q=&market= 통합 검색(국내+미국), market=KR|US 필터
GET  /api/ranking?market=&sector=   랭킹 (KR 즉시 / US 지연로드)
GET  /api/price/{code}      실시간 시세 (KIS→네이버 폴백)
GET  /api/candles/{code}?tf=  일/주/월봉 전환 (day|week|month)
GET  /api/analyze/{code}    종합 분석 (핵심 엔드포인트)
GET  /api/kis/status        KIS 설정 여부
POST /api/kis/config        KIS 키 저장 (공개모드 403)
GET  /api/ai/status         AI 사용 가능 여부
POST /api/ai/report/{code}  AI 리포트 (공개모드 403)
```

### 프론트엔드 (`static/`)

| 파일 | 내용 |
|---|---|
| `index.html` | 3개 뷰(차트·기술·고급분석 1섹션 병합): `#landing`(히어로+관심종목+랭킹) / `#report`(분석) / `#compare-view`(비교) + 비교트레이 + KIS모달 |
| `app.js` | 전 로직. 주요 블록: 통화포맷 → 테마 → 즐겨찾기 → 검색 → 네비 → 랭킹 → 분석/렌더 → 비교 → 밸류에이션 → 고급차트 → 차트 → 재무 → AI → KIS → init |
| `style.css` | CSS 변수 기반 다크/라이트 테마, 글래스모피즘 |

### 구현된 기능 (전부 라이브 검증 완료)

- **종합 점수화**: 가치평가·수익성·성장성·재무안정성·기술적추세·수급심리 6부문 → S/A/B/C/D/F
- **랭킹**: 국내 132 / 미국 147종목, 섹터 필터, Top5 + 더보기(10개씩)
- **종목 분석**: 한줄평 배너 · 게이지/레이더 · 목표주가 · 매수/매도 타점 · 캔들차트(5년) · 기술적분석 · 고급 차트분석 · **밸류에이션 분석** · 투자지표 · 재무 · 수급 · 리포트 · 뉴스 · 동일업종
- **차트 & 기술분석(통합)**: 한 섹션에 캔들차트 + 기술지표 + 고급 차트분석을 묶음. **일/주/월봉 전환**, 기간버튼, **구간 슬라이더**(폭·위치 조절), SMA20/60/120, 지지/저항/목표가, 골든/데드 마커
- **재무 하이라이트**: 매출·영업이익 막대차트 + 연도별 지표표(매출·이익·마진·ROE·EPS·**PER·PBR**·부채비율), 밸류지표는 전년比 방향 색상(▲▼)
- **종목 비교**: 최대 3종목 레이더 겹침 + 지표표(최우수 강조)
- **기타**: 관심종목(localStorage), 다크/라이트 테마, 국내/미국 토글, 모바일 반응형

---

## 3. 지금 작업 중이던 지점 ⭐

### 최근 완료 (2026-07-24)
- 차트 5년 확장(1300봉) + `chart_pro.py` 고급 차트분석 10기법
- 점수화 리밸런싱(바이오·금융 편향 교정)
- **백테스트·스크리너 제거** (사용자 요청) → `app/screener.py` 삭제, `/api/screener` 제거
- **`valuation.py` 밸류에이션 분석 신규** — 아래 5.4d 참조

### ⚠️ 남은 정리 사항
`app/dart.py` 는 **현재 아무 데서도 호출되지 않는다.** 유일한 소비처였던 screener가
삭제됐기 때문. 파일은 남겨뒀으니 둘 중 하나를 택할 것:
- (A) 실적 변곡을 밸류에이션의 "실적방향" 항목에 연결 → DART 키 필요 (미발급 상태)
- (B) 안 쓸 거면 `app/dart.py` 와 `.gitignore` 의 `dart_key.txt` 항목 삭제

---

## 4. 다음 작업 순서 (권장 순)

### 1️⃣ `dart.py` 처리 결정 (위 A/B 중 택1)
DART 키를 받으면 raw 응답부터 확인할 것 — `_parse()` 가 유일한 추정 구간이다:
```python
from app import dart
cm = dart.corp_map(); print(len(cm), cm.get("005930"))
items = dart._fetch_batch([cm["005930"]], 2026, "11013")
print([i for i in items if "영업이익" in i["account_nm"]][:2])
```
키 주입: `dart_key.txt`(gitignore됨) 또는 환경변수 `DART_API_KEY`.
배포 반영은 Render 대시보드 → Environment 에 추가해야 함(코드 푸시로는 안 됨).

### 2️⃣ 밸류에이션 정밀도 개선 (선택)
- 과거 PER 이력이 **실적 3년**뿐 → 진짜 5년을 원하면 외부 소스(FnGuide 등) 필요
- EV/EBITDA가 **미국 전용** (국내 EBITDA 미제공) + 순차입금 미차감 근사
- 과거 컨센서스 추정치 이력이 없어 "추정치 상향 추세"는 미구현 (현재는 전망 성장률로 대체)

### 3️⃣ 기타 백로그
- 미국 종목 ROE·부채비율 부재 → "-" 표시 (네이버 미제공)
- 애널리스트 목표가 분포(최고/평균/최저) 시각화
- 미국주식 원화 병기 (환율 환산)

---

## 5. 핵심 설계 결정 (건드리기 전에 반드시 읽을 것)

### 5.1 데이터 소스 = 네이버 비공식 API (pykrx 아님)
- **pykrx는 쓰지 말 것.** 이 환경에서 `get_market_fundamental` / `get_market_ticker_list` 가
  KRX로부터 **빈 응답**을 받아 실패함(로컬·배포 모두). OHLCV만 작동.
- 대신 네이버 사용. 싱가포르(Render) IP에서도 **정상 작동 검증됨**.
- 국내: `m.stock.naver.com/api` · 미국: `api.stock.naver.com` (자동 라우팅은 `naver.is_us()`)

### 5.2 국내/미국 분기 (`naver.is_us(code)`)
- 판별: **6자리 숫자면 국내**, 그 외는 미국 reutersCode
- 미국 코드 형식: NASDAQ=`AAPL.O`, NYSE=`JPM`(bare) 또는 `ORCL.K` — **불규칙하니 반드시 검색 API로 확인**
  (`https://ac.stock.naver.com/ac?q=TICKER&target=stock` 의 `reutersCode`)

| 데이터 | 국내 | 미국 |
|---|---|---|
| 지표(PER/PBR) | `integration.totalInfos` | `basic.stockItemTotalInfos` |
| 재무 구조 | `{financeInfo:{rowList}}` | `{rowList}` **최상위** |
| 재무 항목 | 매출액/영업이익/ROE/부채비율/유보율 | 매출액/EBIT/당기순이익/ROA (**부채비율·ROE 없음**) |
| 차트 | `fchart` XML | `chart/foreign/item/{rc}/day` JSON |
| 수급·리서치 | 있음 | **없음** (빈 배열 반환) |

### 5.3 값 파싱 함정 ⚠️ (과거 실제 버그)
네이버는 값에 **단위를 붙여서** 줌: `"23.08배"`, `"12,372원"`, `"1,669조 1,125억"`.
→ `analysis.to_num()` 이 콤마·%·배·원·주 제거, `analysis.parse_eok()` 가 조/억 → 억 단위 변환.
**새 필드 파싱 시 반드시 이 두 함수를 쓸 것.** (직접 float() 하면 전부 None 됨)

### 5.4 점수화 로직 (`analysis.py`)
- 부문 가중치: 가치18% · 수익20% · 성장22% · 안정12% · 기술16% · 수급심리12%
- 등급: **S 85+ / A 75+ / B 65+ / C 50+ / D 35+ / F**
- **PEG 보정**(2026-07-24 재조정): 고성장주의 높은 PER을 *일부* 정당화하되 과하지 않게.
  실적 성장 우선, 전망 성장률은 40% 상한, 중앙값 사용, 보정 강도 `value*0.65 + peg*0.35`.
  → 예전엔 `max(성장률들)*0.5` 라 저기반 폭증(예: 적자→흑자 +900%)이 PER 124 종목의
  가치점수를 55까지 끌어올렸음. 지금은 알테오젠(PER124) 가치 25로 정상화.
- **재무안정성 — 금융주 오판 교정**(2026-07-24): 은행·지주는 부채비율이 구조적으로 1000%+ 라
  이를 위험으로 보면 안 됨. **부채비율 >500%는 금융업 추정으로 중립(45) 처리**,
  일반기업은 `_score_low(debt, 50, 300)`. 추가로 **ROE 쿠션**(흑자·양호 ROE면 안정성 하한을
  올림, 상한 60)으로 약점기업만 끌어올림. → 우리금융 안정 13→48, 현대차 D→C.
- **미국 재무안정성 프록시**: 부채 데이터가 없어서 순이익률·ROA 기반으로 산출.
  이게 없으면 미국 전종목이 50점 고정되어 **A등급이 구조적으로 불가능**해짐 (실제 겪은 문제)

### 5.4e 차트 통합·주월봉·구간슬라이더 (2026-07-24)
- **섹션 병합**: 기술적 분석 + 고급 차트분석을 차트와 한 카드(`#chart-card`)로 합침.
  `renderChart`(캔들) → `renderChartPro`(고급, 카드 내부 `#pro-card` div)가 이어 그린다.
- **일/주/월봉**: `GET /api/candles/{code}?tf=day|week|month`. 봉 전환 시 전체 재분석 없이
  캔들만 다시 받아 `drawChart()` 재실행(`switchTimeframe`). 차트 상태는 `chartCtx`에 보관.
  - 국내: fchart가 week/month를 직접 제공.
  - **미국: 네이버가 주/월봉을 안 줌(빈 배열)** → `naver._resample()` 로 일봉을 ISO주차/월 기준
    OHLCV 집계. (시=첫날 시가, 고/저=구간 극값, 종=마지막 종가, 량=합)
- **구간 슬라이더**: 차트 아래 이중 핸들(`#range-slider`)로 보이는 구간의 폭·위치 조절.
  슬라이더→차트(`setVisibleLogicalRange`)와 차트 드래그→슬라이더(`subscribeVisibleLogicalRangeChange`)
  양방향 동기(되먹임은 `rsSync` 플래그로 차단). 기간버튼도 `rsState`를 갱신한다.
- **재무 하이라이트 확장**: `finance_rows` 를 `{표시이름:{unit, series}}` 구조로 바꿔 핵심 지표
  (매출·영업이익·순이익·마진·ROE·EPS·PER·PBR·부채비율)를 연도별로 노출. `_highlight_rows()`
  가 국내/미국 행 이름 차이(영업이익/EBIT 등)를 별칭으로 매칭. 프론트는 unit별 포맷(%·배·원)
  + 밸류지표 전년比 방향 색상.

### 5.4d 밸류에이션 분석 `app/valuation.py` (2026-07-24 신규)
사용자가 지정한 6가지 관점. **1순위는 "현재 FPER ÷ 과거 평균 FPER"**.
가중치: 역사적밸류 30% · PEG 25% · 동종업계 15% · EV/EBITDA 10% · 목표주가 10% · 실적방향 10%

**데이터 한계와 대응(전부 실측으로 확인)**
- 네이버는 연도별 PER/EPS를 **실적 3년 + 컨센서스 1년**만 준다 → "5년 평균"을 그대로 못 만듦.
  → **5년 일봉 × 연도별 EPS로 연도별 평균 PER을 직접 계산**(연말 스냅샷보다 대표성 높음).
  화면에는 실제 사용한 연수(`{n}년 평균`)를 정직하게 표기한다.
- 과거 시점의 컨센서스 EPS는 미제공 → 과거 FPER은 **실현 선행 PER**
  (그 해 평균주가 ÷ **다음 해 실제 EPS**)로 계산. 화면 하단에 이 정의를 명시함.
- EBITDA는 **미국만** 제공(국내 미제공) → 국내는 EV/EBITDA 항목 자체가 빠진다.
- 미국은 재무제표에 **EPS 행이 없다** → 연평균주가 기반 계산 불가.
  대신 제공되는 **PER 행을 그대로** 연도별 PER로 쓰는 폴백(`_per_history_from_per_row`).
  화면에 '공시 PER 사용'으로 근거를 구분 표기한다.

**⚠️ 실제로 밟은 함정 4개 — 되돌리지 말 것**
1. 선행PER 계산에 **컨센서스 EPS를 섞으면 안 된다**. 삼성전자 2025→2026(E)가 1.53배로 나와
   평균을 8.69로 망가뜨렸다. → 다음 해가 **실적인 경우만** 계산.
2. **PEG 성장률 상한 50%**. 저기반 회복(삼성전자 전망 +779%)을 그대로 나누면 PEG 0.01.
3. **동종업계는 평균이 아니라 중앙값** + PER 200배 초과 제외. 주성엔지니어링 1036배가
   업종 평균을 238배로 오염시켰다(→ 중앙값 18.9로 정상화).
4. **미국 단위 불일치**: 시가총액=억 USD, 재무제표=백만 USD. 그대로 나누면 100배 어긋난다
   (AAPL EV/EBITDA 33.8배가 0.34배로 나왔음) → 미국은 EBITDA를 100으로 나눈다.

### 5.4c 고급 차트 분석 `app/chart_pro.py` (2026-07-24 신규)
5년(1300봉) 데이터 기반. 실전 트레이딩 기법 10종을 점수화하고 **기술적추세 부문에 50% 반영**
(`total_evaluation(..., pro)` → `tech_score*0.5 + pro*0.5`). 랭킹은 부하 때문에 미적용(pro=None).

| 기법 | 출처 | 구현 |
|---|---|---|
| 스테이지 분석 | Weinstein | 30주(150일)선 위/아래 + 기울기 → 1~4국면 (2국면=매수, 4국면=회피) |
| 상대강도 RS | O'Neil | 3·6·12개월 벤치마크 초과성과 가중평균. **국내=KOSPI지수 / 미국=SPY** |
| 추세 템플릿 | Minervini | 8개 조건 체크리스트(50/150/200일선 정배열, 52주 위치 등) |
| VCP | Minervini | 3구간 변동폭 축소 + 거래량 마름 |
| 다바스 박스 | Darvas | 박스 상·하단, 돌파/이탈 |
| OBV | Granville | 매집/분산 + **가격 다이버전스** |
| 피보나치 | — | 되돌림 % 및 황금구간 판정 |
| ATR | Wilder | 변동성 %, 5% 초과 시 비중축소 경고 |
| 이격도 | — | 20/60/120일선 괴리율 |

**⚠️ 상대강도 구현 시 두 함정** (둘 다 실제로 밟았음):
1. 종목과 지수는 **거래일수가 달라** 인덱스를 그대로 쓰면 다른 시점을 비교하게 됨
   → 뒤에서부터 공통 길이로 잘라 정렬 (`closes[-n:]`, `bench[-n:]`)
2. 척도를 ±30%p 선형으로 두면 **강세장에서 전 종목이 0/100점으로 포화**됨
   (2026년 코스피 1년 +106%, 삼성전자 +295% — 실제 데이터가 그럼)
   → **로그 스케일** `50 + sign*log10(1+|rs|/5)*32` 로 완만하게 매핑

### 5.4b 기술적(차트) 점수 — 4축 분해(2026-07-24 고도화)
예전엔 base 50에서 단순 가감산이었음. 지금은 **4개 축을 각각 0~100으로 산출 후 가중결합**:
- **추세 45%**: 이평 배열 + **20/60일선 기울기(방향)** + **120일선 완전정배열** + 골든/데드크로스
  (골든크로스는 **거래량 동반 시 가점 강화** 10 vs 6)
- **모멘텀 30%**: MACD 히스토그램 전환/유지 + RSI(과열75/70·과매도25/30, **50~70 상승중립 가점**)
- **위치 15%**: 볼린저 %B + **52주 위치**(신고가권/신저가권)
- **거래량 10%**: 거래량 급증이 **상승 방향이면 가점, 하락 방향이면 감점**(매물 출회)
- 반환에 `score_parts`(4축 점수)·`ma20_slope`·`ma60_slope` 추가 → 프론트가 4축 바로 표시
- **지지/저항**: 단순 min/max 아님. `_support_resistance()` 가 스윙 저점/고점 군집화 후
  **현재가 아래 최근접 지지 / 위 최근접 저항**을 선택하고 `지지 < 현재가 < 저항`을 강제 보장

### 5.5 공개 배포 모드 (`STOCKLENS_PUBLIC=1`, Dockerfile에 내장)
- KIS 키 저장 **403 차단** (낯선 사람이 개인 브로커 키를 서버에 저장 못 하게)
- AI 리포트 **403 차단** (소유자 API 키로 비용 발생 방지)
- IP별 레이트리밋 활성
- 로컬 실행 시엔 이 값이 없어 전 기능 사용 가능

### 5.6 백그라운드 집계 전략 (무료 서버 부하 분산)
| 대상 | 시점 | 주기 | 소요 |
|---|---|---|---|
| 국내 랭킹 | 서버 시작 시 즉시 | 30분 | ~80초 |
| 미국 랭킹 | **첫 요청 시 지연 시작** | 30분 | ~200초 |
- 집계 중이면 API가 `computing:true` 반환 → 프론트가 5~6초 간격 폴링하며 "집계 중" 표시
- Render 무료는 15분 미접속 시 슬립 → 깨어나면 재집계됨(정상)

---

## 6. 실행 / 테스트 방법

### 로컬 실행
```bash
cd C:\Users\권태균\stocklens
pip install -r requirements.txt
python -m uvicorn main:app --port 8899
```
→ http://127.0.0.1:8899

`start.bat` 더블클릭해도 됨 (브라우저 자동 오픈 + 휴대폰 접속 주소 표시).

**⚠️ `--reload` 없음 → 백엔드(.py) 수정 시 서버를 반드시 재시작해야 반영됨.**

### 선택 기능 활성화 (로컬 전용)
```powershell
# AI 리포트
pip install anthropic
$env:ANTHROPIC_API_KEY = "sk-ant-..."
# KIS 실시간 시세는 화면 우상단 ⚙ 버튼에서 키 입력
```

### API 빠른 테스트
```powershell
$b = "http://127.0.0.1:8899"
Invoke-RestMethod "$b/api/analyze/005930"     # 국내
Invoke-RestMethod "$b/api/analyze/AAPL.O"     # 미국
Invoke-RestMethod "$b/api/ranking?market=US"  # 미국 랭킹(첫 호출은 집계 시작)
```
> PowerShell은 **한글 키 접근이 안 됨**(`$a.total.categories.'가치평가'` → 빈값).
> 부문 점수 확인은 Python 스크립트로 할 것.

### 프론트엔드 검증 (중요)
- **스크린샷 도구는 이 프로젝트에서 계속 타임아웃됨** (외부 CDN 로드 대기 때문).
  → `javascript_tool` 로 **DOM을 직접 eval 검증**할 것. 이 방식은 안정적으로 동작함.
- 예: `document.querySelectorAll('#rank-list .rank-row').length`, `getComputedStyle(...)` 등
- 분석 렌더는 **8~9초** 기다려야 완료됨 (7초는 부족했던 사례 있음)

### 🚨 CSS/JS 수정 시 반드시 캐시 버전 올리기
`static/index.html` 의 `?v=11` 을 **+1** 할 것 (css·js 둘 다).
**이걸 빼먹어서 "고쳤는데 반영이 안 된다"고 여러 번 헤맴.** 현재 v11.

### 배포
```bash
git add -A && git commit -m "..." && git push origin main
```
→ Render 자동 재배포 (2~4분). 확인:
```powershell
(Invoke-WebRequest "https://stocklens-mpr6.onrender.com/" -UseBasicParsing).Content -match 'app\.js\?v=11'
```
> 사용자 기본 기대: **수정 요청 = 로컬 구현에서 끝내지 말고 배포·반영확인까지** 완료.

---

## 7. 알려진 제약 / 미해결

| 항목 | 내용 |
|---|---|
| 미국 ROE·부채비율 | 네이버 미제공 → "-" 표시. 안정성은 프록시로 대체 |
| 과거 PER 이력 | 네이버가 **실적 3년**만 제공 → '5년 평균'은 실제 사용 연수를 표기해 정직하게 노출 |
| EV/EBITDA | 미국 전용(국내 EBITDA 미제공) + 순차입금 미차감 근사 |
| 무료 서버 슬립 | 15분 미접속 시 슬립, 재기동 시 첫 로딩 ~50초 + 랭킹 재집계 |
| 데이터 출처 | 네이버 **비공식** API. 스펙 변경 시 파싱 깨질 수 있음(과거 실제 발생) |

# StockLens 인계 문서 (HANDOFF)

> 이 문서만 읽고 바로 이어서 개발할 수 있도록 작성됨.
> 최종 갱신: 2026-08-14 · 캐시버전 **v40** · 3대 구조 개편(홈/종목분석/포트폴리오) Phase1+2 완료,
> "오늘의 AI 투자기회" 랭킹과 차별화, 종목분석 탭 무반응 버그 수정

---

## 1. 프로젝트 개요

**StockLens** — 국내(코스피/코스닥) + 미국 주식 종합 분석 웹앱.
종목을 6개 부문으로 점수화(S~F 등급)하고, AI 최종판단(5단계+확신도)·목표주가·적정매수가·
가격별 매수전략·차트(5년)·고급 차트분석·밸류에이션 분석·뉴스·AI 관리형 포트폴리오를 제공.

| 항목 | 값 |
|---|---|
| 로컬 경로 | `C:\Users\권태균\stocklens` |
| **라이브 URL** | **https://stocklens.161-33-201-126.nip.io** (Render는 폐기됨, 아래 참고) |
| GitHub | https://github.com/kwontaegyun2-dotcom/Repository-name-stocklens (main 브랜치) |
| 배포 서버 | 오라클 클라우드 인스턴스 `161.33.201.126` (train-bot과 동일 서버, 리버스프록시로 분기) |
| 스택 | FastAPI + 바닐라 JS (프레임워크·빌드 없음), 외부 CDN: lightweight-charts |

> ⚠️ **별개 프로젝트 주의**: `C:\Users\권태균\stock-analyzer` 는 예전 Streamlit 분석기. 무관하니 건드리지 말 것.

> ⚠️ **Render는 폐기됨(2026-08-13)**: 예전엔 Render(`stocklens-mpr6.onrender.com`)에도 배포돼 있었으나
> 사용자가 Render 대시보드에서 직접 일시정지시켰다. **지금은 오라클이 유일한 라이브 배포처**다.
> git push는 GitHub 백업 목적으로 계속 하지만, "자동 재배포"는 더 이상 없다 — 오라클은
> **수동 SCP+SSH 배포**가 필수다(아래 6장).

---

## 2. 전체 아키텍처

### 백엔드 (`app/`)

| 파일 | 역할 |
|---|---|
| `main.py` | FastAPI 엔트리. 전 엔드포인트, 공개모드 게이팅, 레이트리밋 |
| `naver.py` | 네이버 비공식 API 클라이언트 (국내/미국 자동 라우팅) |
| `analysis.py` | 분석 엔진(기술적·펀더멘털·감성·점수화) + **`final_verdict()`**(AI 5단계 판단) |
| `ranking.py` | 국내 182 / 미국 190종목 백그라운드 채점·캐싱 (`UNIVERSE`/`US_UNIVERSE`) |
| `chart_pro.py` | 고급 차트 분석 10기법(스테이지·RS·추세템플릿·VCP·OBV 등) |
| `valuation.py` | 밸류에이션 6항목(역사적PER·PEG·동종업계·EV/EBITDA·목표주가·실적방향) + `fair_buy_price()` |
| `themes.py` | 7개 테마(AI반도체/2차전지/바이오헬스케어/빅테크플랫폼/자동차모빌리티/금융/엔터미디어) 큐레이션 |
| `anomaly.py` | 이상징후 탐지(저평가 확대 / 단기 과열) — 랭킹 캐시 재사용, 신규 계산 없음 |
| `dart.py` | DART 주요계정 조회 (키 없으면 자동 비활성, **현재 어디서도 호출 안 됨** — 사용 안 함) |
| `kis.py` | 한국투자증권 실시간 시세 (공개모드에서 403 차단) |
| `ai.py` | Claude AI 심층 리포트 (공개모드에서 403 차단) |
| `auth.py` | 회원가입/로그인 — 표준 라이브러리만 사용(sqlite3+hashlib+hmac), 아이디/비밀번호 방식 |
| `push.py` | VAPID 웹푸시 발송 (ddok-alarm 프로젝트 패턴 재사용) |
| `watch.py` | 관심종목 매수기회 알림(15분 주기) + **push_subs 테이블 소유**(다른 알림 기능도 여기 구독을 공용으로 씀) |
| `portfolio.py` | 내 포트폴리오 — 평가금액/비중/위험/AI 리밸런싱 등 (아래 3장에서 상세) |
| `portfolio_alert.py` | 포트폴리오 자동 매수/매도 신호 웹푸시(30분 주기, `watch.py`의 구독 인프라 재사용) |
| `event_alert.py` | 관심종목 실적·뉴스·수급 급변 알림(20분 주기, `watch.py` 구독 인프라 재사용) — 아래 4장 참고 |

### API 엔드포인트 전체 목록

```
GET  /                          메인 HTML
GET  /api/search?q=&market=     통합 검색(국내+미국)
GET  /api/ranking?market=&sector=  랭킹 (ai_verdict/target_price 포함)
GET  /api/themes , /api/themes/{name}
GET  /api/anomalies
GET  /api/price/{code}          실시간 시세 (KIS→네이버 폴백)
GET  /api/candles/{code}?tf=    일/주/월봉
GET  /api/analyze/{code}        종합 분석 (핵심 엔드포인트, ai_verdict 포함)
GET  /api/kis/status , POST /api/kis/config   (공개모드 403)
GET  /api/ai/status , POST /api/ai/report/{code}   (공개모드 403)
POST /api/auth/signup|login|logout , GET /api/auth/me
POST /api/auth/change-password
GET  /api/admin/users           (관리자 전용)
GET  /api/push/key , POST /api/push/subscribe|unsubscribe , POST /api/push/test
GET  /api/watch , POST/DELETE /api/watch/{code}
GET  /api/portfolio , POST/DELETE /api/portfolio/{code}
GET  /sw.js                     서비스워커 (웹푸시용)
```

### 프론트엔드 (`static/`)

| 파일 | 내용 |
|---|---|
| `index.html` | **3-tab 구조**: `#landing`(홈) / `#report`(종목분석) / `#portfolio-view`(포트폴리오) + `#compare-view`/`#admin-view` + 모달들 |
| `app.js` | 전 로직. 상단 `#main-nav`가 3개 뷰 전환을 담당(`goHome()`/`analyze()`/`showPortfolio()`) |
| `style.css` | CSS 변수 기반 다크/라이트 테마 |

회원 DB(`users.db`)는 `STOCKLENS_DATA_DIR`(오라클: `/opt/stocklens-data`)에 저장 —
**코드 배포 경로(`/opt/stocklens`) 밖**이라 재배포가 회원 데이터를 안 건드린다.

---

## 3. 지금까지 구현된 기능 (전부 라이브 검증 완료, v38)

### 3.1 핵심 분석 엔진
- 6부문 점수화(가치평가18%·수익성20%·성장성22%·재무안정성12%·기술적추세16%·수급심리12%) → S~F 등급
- **AI 최종판단(5단계+확신도)**: `analysis.final_verdict()` — 종합점수를 매수(80+)/분할매수(65+)/
  보유(45+)/분할매도(30+)/매도 5단계로 변환. **확신도(50~95%)는 점수 재표시가 아니라, 6개 부문점수
  (+가능하면 밸류에이션 점수·컨센서스 상승여력)가 판단 방향과 얼마나 일치하는지를 잰 것** — 100%는
  과신 방지를 위해 절대 안 씀. 상세페이지(main.py)는 valuation까지 반영해 신호가 많고, 랭킹은
  무거운 재계산을 피해 total+cons만 사용.
- 밸류에이션 6항목 + `fair_buy_price()`(보수적/기준/낙관적 3단계 적정매수가) + 5단계 가격구간표
  (적극매수/매수/분할매수/관망/고평가) + **단계별 매수전략**(1차=optimistic/2차=base/3차=conservative,
  30/30/40% 배분)
- 이상징후 탐지(신호 3개 이상 겹칠 때만 플래그), 테마별 투자매력도 TOP10

### 3.2 3대 구조 개편 (Phase 1+2, 2026-08-14)
사용자가 "기능을 더 넣기보다 구조를 바꾸는 게 먼저"라며 전체 사이트를 **홈 / 종목분석 / 포트폴리오**
3화면 체계로 재구성 요청. Phase1(반드시)+Phase2(차별화) 완료, Phase3(알림 강화)는 대기 중.

- **① 네비게이션**: 상단 `#main-nav`(홈|종목분석|포트폴리오) — 기존 뷰 전환 함수 재사용.
- **② 홈 재배치**: Today's Pick(적정매수가·목표가·상승여력) → 왜 주목하는가(6개 부문점수+RSI
  과열경고, 신규계산 없음) → 오늘의 투자기회 TOP5 → 테마 → 관심종목(실시간가+AI판단 표시) →
  이상징후 → 전체랭킹. Today's Pick 1종목에 한해서만 `/api/analyze` 추가호출로 진짜 적정매수가를
  가져옴(랭킹 전체엔 안 넣음 — 무거워짐).
- **③ 종목상세 최상단**: 흩어져 있던 AI최종판단/한줄평/저평가문장/핵심지표/근거/가격별 투자전략을
  헤더 바로 아래 `#highlight-card` 하나로 응집(순수 HTML 이동, JS 로직 안 건드림). **탭 구조
  (종합/차트/밸류에이션/재무/수급/뉴스/리포트/AI분석) 전환은 의도적으로 범위 제외** — 리스크 큰
  대규모 변경이라 필요시 별도 요청으로 진행할 것.
- **④ 단계별 매수 전략**: 위 3.1 참고, 순수 프론트 렌더링.
- **⑤ AI 포트폴리오 진단 강화**: 포트폴리오 종합점수를 등급(S~F)으로 변환(`analysis.GRADE_TABLE`
  재사용), '오늘 하루 손익' 추가, 보유종목 테이블에 AI판단·권장비중 컬럼 추가.
- **⑥ AI 리밸런싱**: 아래 3.3 참고.

### 3.3 내 포트폴리오 (`app/portfolio.py`) — 이 앱의 핵심 차별화 기능
⚠️ **v1은 국내 종목만 지원**(환율 소스가 없어 해외 보유분은 "제외됨"으로 명시, 억지로 원화 환산 안 함).

- 평가금액/비중/평가손익(평균단가 입력 시)/오늘 하루 손익/포트폴리오 등급(S~F)
- **변동성·최대낙폭**: 개별 종목 지표 가중평균이 아니라 보유종목 공통거래일 종가를 실제 합산해
  포트폴리오 가치 시계열을 재구성 후 계산(분산효과 반영된 진짜 숫자)
- **오늘의 진단**(`_today_actions`): 🔴비중과다(균등분산 대비 1.5배↑) / 🟡매수타이밍악화(전일대비
  점수 -8↓, `snapshot_score`/`snapshot_date` 컬럼으로 하루 1회만 갱신) / 🟢추가매수기회(적정매수가
  대비 -5%↓) / 🔴매도신호(목표가도달·RSI과열·외국인순매도전환 중 **2개 이상** 겹칠 때만)
- **위험 자동탐지**(`_risk_flags`): 종목쏠림(30%↑, 위험기여도%까지 공분산행렬로 계산)/업종쏠림
  (50%↑)/상관관계 클러스터(상관계수 0.7↑ 종목을 union-find로 묶어 합산비중 20%↑면 "사실상 같은 베팅")
- **실질 노출**(`_theme_exposure`): `themes.py` 큐레이션 재사용, ETF 룩스루는 데이터 소스 없어 의도적 제외
- **종목간 상관관계**: Pearson 상관계수 행렬(최근 1년 공통거래일 수익률)
- **AI 리밸런싱**(`_recommend_weights`): 종목별 AI판단(tier)에 배점(매수1.5/분할매수1.2/보유1.0/
  분할매도0.6/매도0.3)해 권장비중 계산, 종목당 상한 적용.
  ⚠️ **실제로 잡은 버그**: 상한을 고정 30%로 두면 종목 수가 적을 때 수학적으로 불가능한 조합이 나옴
  (3종목 중 2개가 상한에 걸리면 나머지 1개가 무조건 40%를 떠안는데, 그게 '매도' 등급이면 매도등급이
  최고비중을 받는 모순). **상한을 `max(30%, 100%/종목수×1.4)`로 동적 완화**하고 재분배도 반복(최대
  4회) 수렴시키도록 수정 — 되돌리지 말 것.
- **자동 알림**(`portfolio_alert.py`): 30분마다 보유종목 있는 전 사용자 재평가 → 매수 적정구간
  진입/매도 신호 시 웹푸시(`watch.send_to_user()` 재사용, 별도 옵트인 UI 불필요 — 관심종목 알림과
  같은 구독 사용). 같은(user,code,signal) 조합은 24시간 쿨다운.

### 3.4 회원/알림 인프라
- 아이디/비밀번호 인증(이메일 아님). **관리자 계정은 서버가 `admin`/`admin1234`로 자동 부트스트랩.**
- 웹푸시(VAPID): 관심종목 매수기회 알림(15분 주기) + 포트폴리오 매수/매도 신호(30분 주기)가 같은
  구독 인프라(`push_subs` 테이블, `watch.py` 소유이나 전역 공용)를 공유.

### 3.5 UI 버그·차별화 수정 (2026-08-14, v39/v40)
- **v39**: 종목 하나를 본 뒤 홈으로 돌아가면 `currentCode`가 null로 리셋되는데, 이 상태에서
  "종목분석" 탭을 누르면 `goHome()`이 조용히 재실행되며 "홈" 탭만 계속 활성화된 채로 남아
  버튼이 무반응처럼 보였다. 검색창에 펄스 하이라이트 애니메이션(`.search-wrap.pulse-hint`)을
  추가해 "먼저 검색하라"는 피드백을 명확히 줌.
- **v40**: 사용자가 "AI 투자기회 TOP5가 실시간 랭킹 상위 5개를 그대로 보여주는 것과 다를 게
  없다"고 지적 — 실제로 `renderTodayPick(d.items)`가 `/api/ranking`의 정렬 결과를 그대로
  `slice(0,5)`만 하고 있었다(진짜 중복). **`app.js`의 `pickOpportunities()`로 교체**: 종합점수
  상위 20개 후보군 안에서 극단적 과열(RSI 78+) 제외 후, 상승여력·RSI·PER 이력비·외국인수급·
  실적전망(`op_growth_fwd`, anomaly.py와 동일하게 50% 표시캡)을 반영한 `opportunityScore()`로
  재정렬. 각 종목에 왜 뽑혔는지 근거 칩(`opportunityReasons()`)도 표시해 차이가 눈에 보이게 함.
  **백엔드 추가 호출 없음** — `/api/ranking`이 이미 주는 필드만 프론트에서 재계산(랭킹 백그라운드
  루프에 무거운 계산을 얹지 말라는 CLAUDE.md 규칙 준수).

---

## 4. 다음 작업 — Phase 3 (알림 강화)

사용자가 제시한 ⑦가격알림 ⑧적정매수가 진입알림 ⑨AI매도신호 ⑩실적·뉴스·수급 급변알림 중
**⑧⑨는 `portfolio_alert.py`, ⑩은 `event_alert.py`로 구현 완료(2026-08-14, 라이브 배포됨).**
실제로 남은 건:
- 기존 알림 조건·문구를 사용자가 그린 목업(`🔔 08:32 삼성전자 적정매수가 진입` 같은 타임스탬프형
  알림 로그)에 맞게 다듬는 작업에 가까울 가능성이 큼
- ⑦ 순수 가격 알림(사용자가 지정한 가격 도달 시)은 아직 없음 — 새 기능

### ⑩ 실적·뉴스·수급 급변 알림 (`app/event_alert.py`, 신규)
관심종목(`watch.py`의 watchlist·push_subs 재사용, 별도 옵트인 UI 없음)을 20분마다 재분석해
**직전 체크 대비 변화(델타)** 를 감지한다 — 절대 수치가 아니라 "새로 바뀐 것"만 알림:
- 📈📉 실적 급변: `metrics.op_growth`(최근 실적 영업이익 YoY)가 갱신되며 ±30% 이상일 때
- 📰 뉴스 급변: 새 기사(URL 기준)가 뜨고 감성이 긍정/부정으로 뚜렷할 때(중립 제외)
- 🔄 수급 급변: 외국인+기관 최근 5일 순매수 합의 부호가 매수↔매도로 전환될 때(국내 전용,
  미국은 `naver.trend()`가 데이터를 안 줘서 flows가 항상 비어 자동 스킵됨)

⚠️ **그 종목의 첫 체크(state 없음)는 기준값만 저장하고 알림을 보내지 않는다** — 안 하면 배포/서버
재시작 직후 기존에 이미 알던 상태 전체가 "새 이벤트"로 오탐돼 스팸이 된다. 종목당 분석은 관심등록자
수와 무관하게 1회만(watchlist를 code로 그룹핑) — 등록자 전원에게는 그 결과만 나눠 보낸다.
쿨다운은 `portfolio_alert.py`와 동일하게 (user, code, signal) 조합 24시간.

착수 전에 사용자에게 "Phase3 이어서 할까요?" 확인 필요 — 직전 대화에서 그렇게 안내해둔 상태.

### 기타 백로그
- `app/dart.py` 미사용 상태 유지 중(밸류에이션 실적방향에 연결하거나, 안 쓸 거면 삭제 검토)
- 탭 구조로 종목상세 페이지 전환(Phase1에서 의도적으로 제외한 항목)
- 미국 종목 원화 병기(환율 환산) — 포트폴리오 해외지원의 전제조건이기도 함

---

## 5. 핵심 설계 결정 & 함정 (건드리기 전에 반드시 읽을 것)

### 5.1 데이터 소스 = 네이버 비공식 API (pykrx 아님)
pykrx는 이 환경에서 KRX 펀더멘털/티커 엔드포인트가 빈 응답 → 쓰지 말 것. 국내:
`m.stock.naver.com/api` · 미국: `api.stock.naver.com` (`naver.is_us(code)`로 자동 라우팅,
6자리 숫자=국내). 미국 코드 형식 불규칙(`AAPL.O`/`JPM`/`ORCL.K`) — 검색 API로 반드시 확인.

### 5.2 값 파싱 함정
네이버는 값에 단위를 붙여 준다(`"23.08배"`, `"12,372원"`, `"1,669조 1,125억"`).
`analysis.to_num()`/`analysis.parse_eok()` 필수 — `float()` 직접 쓰면 전부 None.

### 5.3 밸류에이션 4대 함정 (`app/valuation.py`, 전부 실측으로 확인)
1. 선행PER에 **컨센서스 EPS를 섞지 않는다** — 다음 해가 실적인 경우만 계산.
2. **PEG 성장률 상한 50%** — 저기반 회복(적자→흑자 등)을 그대로 나누면 PEG 0.01처럼 무의미해짐.
3. **동종업계는 중앙값** + PER 200배 초과 제외 — 이상치 1종목이 업종평균을 통째로 오염시킴.
4. **미국 단위 환산**: 시총=억 USD, 재무제표=백만 USD. EBITDA는 100으로 나눠야 함.
5. `fair_buy_price()`는 각 관점의 괴리배수를 **현재가 0.5~1.6배로 clamp** — 안 하면 PEG 환산식이
   폭주해 적정가가 현재가의 3배 이상으로 나올 수 있음(실제 삼성전자에서 발견).

### 5.4 점수화 편향 재도입 금지 (`app/analysis.py`)
- PEG 보정에 성장률을 그대로 곱하지 않는다(저기반 폭증 방지, 상한 40~50%·중앙값 사용)
- 부채비율 >500%는 금융업 구조로 보고 중립 처리(은행·지주를 재무위험으로 깎지 않음)
- 점수 로직 변경 시 **반드시 섹터가 다른 종목 묶음으로 재검증**(바이오·금융·자동차·통신 등)

### 5.5 AI 리밸런싱 동적 상한 (`app/portfolio.py`, 5.3장 위 참고)
고정 30% 상한은 보유종목이 적을 때 모순을 만든다 — `max(30%, 100%/종목수×1.4)`로 동적 완화 필수.

### 5.6 랭킹 백그라운드 집계 & 서버 부하
- 국내 즉시 시작 / 미국 90초 지연 시작, 30분마다 재계산, 20종목마다 부분결과 공개(`computing:true`
  폴링). **배포(재시작) 직후 몇 분간 `/api/analyze`가 19~30초까지 느려질 수 있음** — 랭킹 재계산이
  네트워크를 다 써서 생기는 경합, 코드 버그 아님. "느리다" 보고를 받으면 먼저
  `/api/ranking?market=US`의 `computing` 값부터 확인할 것.
- 새 무거운 집계는 **랭킹의 `_score()` 배경 루프에 절대 추가하지 말 것**(180+190종목마다 비용이
  곱해짐) — 온디맨드 단일 종목 추가호출(홈 Today's Pick 방식) 또는 캐시 재사용만 쓸 것.

### 5.7 공개 배포 모드 (`STOCKLENS_PUBLIC=1`)
KIS 키 저장·AI 리포트 403 차단, IP별 레이트리밋 활성. 로컬 실행 시엔 이 값이 없어 전 기능 사용 가능.

---

## 6. 실행 / 배포 방법

### 로컬 실행
```bash
cd C:\Users\권태균\stocklens
python -m uvicorn main:app --port 8899
```
→ http://127.0.0.1:8899 · **`--reload` 없음 → 백엔드(.py) 수정 시 서버 재시작 필수.**

### 🚨 CSS/JS 수정 시 반드시 캐시 버전 올리기
`static/index.html`의 `style.css?v=N` / `app.js?v=N`을 **둘 다 +1**. 현재 v38.

### 오라클 배포 (Render 자동배포 없음 — 수동 배포 필수)
SSH 키: `C:\Users\권태균\Downloads\ssh-key-2026-08-06.key` (계정 `ubuntu@161.33.201.126`).
**매번 권한 재설정 필요**(Windows가 리셋함):
```powershell
icacls $key /inheritance:r
icacls $key /grant:r "$($env:USERNAME):(R)"
```
⚠️ **ssh/scp는 반드시 PowerShell 도구로 실행할 것** — Bash 도구는 auto-mode 분류기가 원격 접속을
위험 작업으로 보고 차단한다.

변경된 파일만 개별 scp(전체 tar 불필요, 더 빠르고 안전):
```powershell
$key = "C:\Users\권태균\Downloads\ssh-key-2026-08-06.key"
scp -i $key -o StrictHostKeyChecking=no "C:\Users\권태균\stocklens\<파일>" "ubuntu@161.33.201.126:/tmp/<파일명>"
ssh -i $key -o StrictHostKeyChecking=no ubuntu@161.33.201.126 @'
sudo cp /tmp/<파일명> /opt/stocklens/<원래경로>/<파일명>
sudo chown stocklens:stocklens /opt/stocklens/<원래경로>/<파일명>
sudo systemctl restart stocklens
sudo systemctl is-active stocklens
'@
```
라이브 확인: `curl -s "https://stocklens.161-33-201-126.nip.io/" | grep -o 'style.css?v=[0-9]*'`

GitHub push도 계속 병행(백업 목적):
```bash
git add -A && git commit -m "..." && git push origin main
```

### 프론트엔드 검증
- **스크린샷 도구는 계속 타임아웃됨**(외부 CDN 로드 대기) → `javascript_tool`로 DOM 직접 eval 검증할 것.
- 종목 분석 렌더는 **8~9초** 대기 후 확인.
- PowerShell은 **한글 키 접근 불가**(`$a.total.categories.'가치평가'` → 빈값) — 부문점수 확인은 Python으로.
- PowerShell `Invoke-RestMethod -Body`는 한글을 콘솔 코드페이지 때문에 `?`로 깨뜨림 — 실제 버그인지
  헷갈리면 브라우저 fetch로 재현할 것(인코딩 문제가 가짜 단서였던 사례 있음).

### 삭제/정리 작업 시 원칙
DELETE API 호출 후 응답 `{"ok":true}`만 믿지 말고 **반드시 후속 GET으로 실제 상태 재확인**할 것
(과거 이렇게 안 해서 정리 누락이 뒤늦게 발견된 사례 있음).

---

## 7. 알려진 제약 / 미해결

| 항목 | 내용 |
|---|---|
| 포트폴리오 해외종목 | 환율 소스 없어 v1은 국내 종목만 지원, 해외 보유분은 "제외됨"으로 표시 |
| 포트폴리오 ETF 룩스루 | 데이터 소스 없어 의도적으로 미지원(QQQ 내부 NVDA 비중 역산 등) |
| 웹푸시 실제 수신 | 자동화 브라우저는 Notification 권한이 항상 거부 상태라 백엔드 로직만 단위테스트로 검증됨 — 실사용 여부는 사용자가 실기기에서 확인 필요 |
| 미국 ROE·부채비율 | 네이버 미제공 → "-" 표시, 안정성은 프록시로 대체 |
| 과거 PER 이력 | 네이버가 실적 3년만 제공 → 실제 사용 연수를 정직하게 표기 |
| EV/EBITDA | 미국 전용(국내 EBITDA 미제공) + 순차입금 미차감 근사 |
| 실적발표 예정일 | 네이버 API에 필드 자체가 없음(확인됨) — 관련 기능은 사용자 요청으로 전부 제거됨(2026-08-14) |
| 데이터 출처 | 네이버 비공식 API. 스펙 변경 시 파싱 깨질 수 있음(과거 실제 발생) |

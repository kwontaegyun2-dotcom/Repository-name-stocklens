# StockLens 인계 문서 (HANDOFF)

> 이 문서만 읽고 바로 이어서 개발할 수 있도록 작성됨.
> 최종 갱신: 2026-08-21 · 캐시버전 **style.css v88 · app.js v97** · 오늘의 PICK
> 날짜별 로테이션 + 관심종목 모바일 레이아웃 재정렬(3.27장) · 아이콘이 화면 전체에서
> 잘려 보이던 근본 원인(`<svg class="i">`에 `viewBox` 누락) 수정 + 네비 아이콘
> 통일·관심종목 버튼 겹침·포트폴리오 종목추가 위치 3건(3.26장) · UI/UX 진단보고서
> 반영 2부 — 홈 3영역 재편·전용페이지(/watchlist·/screener·/portfolio) 신설·용어
> 통일 8종·이모지→SVG 아이콘 28심볼(3.25장) · UI/UX 진단보고서 반영 1부 — 디자인
> 토큰·라이트모드 대비 재조정·sticky 탭바·접근성(3.24장) · 4차 진단리포트 P0+P1 —
> 볼륨프로파일/스윕/컨플루언스 신뢰도 보강(3.23장) · 기술점수에 컨플루언스 반영,
> 실시간 시세 지연 단축, 오늘의 PICK 박스 크기 통일(3.22장) · 트레이딩엔진 설계서
> 반영 — AVWAP·스윕·볼륨프로파일·오더플로우·컨플루언스 엔진(3.21장). **이 구간
> (3.21~3.27) 동안 `HANDOFF.md` 갱신이 밀려 있었음 — v65 이후 v88/v97까지의
> 커밋은 전부 이번에 소급 정리한 것이므로, 각 장의 세부 수치·근거 서술은 실제 세션
> 당시 기록이 아니라 커밋 로그·대화 요약을 근거로 재구성했다는 점을 감안할 것.**
> 이전(3.20장까지): 3차 진단리포트 P0 7건(적자기업 성장성 왜곡, 판단의 가격 무시,
> 등급경계 불일치, 포트폴리오 리스크감점 과잉교정 등) · AI 최종판단 8단계 세분화 ·
> 2차 진단리포트 P0 8건(알림 권한요청 무응답 등) · 고급 차트 4대 기법(AVWAP·Sweep·
> Volume Profile·Order Flow) · URL 라우팅 도입 · 외부 진단리포트(Opus 검증) P0 —
> 컨센서스 이상치 검증 레이어 신설 · 3대 구조 개편(홈/종목분석/포트폴리오) · 포트폴리오
> 미국 종목 지원 · 종목상세 탭 구조화(UX 개편 12개 항목 전부 완료) · 캔들차트 개선.
> 상세는 3.1~3.20장 참고.

---

## 1. 프로젝트 개요

**StockLens** — 국내(코스피/코스닥) + 미국 주식 종합 분석 웹앱.
종목을 6개 부문으로 점수화(S~F 등급)하고, AI 최종판단(5단계+확신도)·목표주가·적정매수가·
가격별 매수전략·차트(5년)·고급 차트분석·밸류에이션 분석·뉴스·AI 관리형 포트폴리오를 제공.

| 항목 | 값 |
|---|---|
| 로컬 경로 | `C:\Users\권태균\stocklens` |
| **라이브 URL** | **https://stocklens.161-33-201-126.nip.io** (Render는 폐기됨, 아래 참고) |
| GitHub | https://github.com/kwontaegyun2-dotcom/Repository-name-stocklens (main 브랜치) |
| 배포 서버 | 오라클 클라우드 인스턴스 `161.33.201.126` (train-bot과 동일 서버, 리버스프록시로 분기) |
| 스택 | FastAPI + 바닐라 JS (프레임워크·빌드 없음), 외부 CDN: lightweight-charts |

> ⚠️ **별개 프로젝트 주의**: `C:\Users\권태균\stock-analyzer` 는 예전 Streamlit 분석기. 무관하니 건드리지 말 것.

> ⚠️ **Render는 폐기됨(2026-08-13)**: 예전엔 Render(`stocklens-mpr6.onrender.com`)에도 배포돼 있었으나
> 사용자가 Render 대시보드에서 직접 일시정지시켰다. **지금은 오라클이 유일한 라이브 배포처**다.
> git push는 GitHub 백업 목적으로 계속 하지만, "자동 재배포"는 더 이상 없다 — 오라클은
> **수동 SCP+SSH 배포**가 필수다(아래 6장).

---

## 2. 전체 아키텍처

### 백엔드 (`app/`)

| 파일 | 역할 |
|---|---|
| `main.py` | FastAPI 엔트리. 전 엔드포인트, 공개모드 게이팅, 레이트리밋 |
| `naver.py` | 네이버 비공식 API 클라이언트 (국내/미국 자동 라우팅) |
| `analysis.py` | 분석 엔진(기술적·펀더멘털·감성·점수화) + **`final_verdict()`**(AI 5단계 판단) |
| `ranking.py` | 국내 182 / 미국 190종목 백그라운드 채점·캐싱 (`UNIVERSE`/`US_UNIVERSE`) |
| `chart_pro.py` | 고급 차트 분석 10기법(스테이지·RS·추세템플릿·VCP·OBV 등) |
| `valuation.py` | 밸류에이션 6항목(역사적PER·PEG·동종업계·EV/EBITDA·목표주가·실적방향) + `fair_buy_price()` |
| `themes.py` | 7개 테마(AI반도체/2차전지/바이오헬스케어/빅테크플랫폼/자동차모빌리티/금융/엔터미디어) 큐레이션 |
| `anomaly.py` | 이상징후 탐지(저평가 확대 / 단기 과열) — 랭킹 캐시 재사용, 신규 계산 없음 |
| `dart.py` | DART 주요계정 조회 (키 없으면 자동 비활성, **현재 어디서도 호출 안 됨** — 사용 안 함) |
| `kis.py` | 한국투자증권 실시간 시세 (공개모드에서 403 차단) |
| `ai.py` | Claude AI 심층 리포트 (공개모드에서 403 차단) |
| `auth.py` | 회원가입/로그인 — 표준 라이브러리만 사용(sqlite3+hashlib+hmac), 아이디/비밀번호 방식 |
| `push.py` | VAPID 웹푸시 발송 (ddok-alarm 프로젝트 패턴 재사용) |
| `watch.py` | 관심종목 매수기회 알림(15분 주기) + **push_subs 테이블 소유**(다른 알림 기능도 여기 구독을 공용으로 씀) |
| `portfolio.py` | 내 포트폴리오 — 평가금액/비중/위험/AI 리밸런싱 등 (아래 3장에서 상세) |
| `portfolio_alert.py` | 포트폴리오 자동 매수/매도 신호 웹푸시(30분 주기, `watch.py`의 구독 인프라 재사용) |
| `event_alert.py` | 관심종목 실적·뉴스·수급 급변 알림(20분 주기, `watch.py` 구독 인프라 재사용) — 아래 4장 참고 |

### API 엔드포인트 전체 목록

```
GET  /                          메인 HTML
GET  /api/search?q=&market=     통합 검색(국내+미국)
GET  /api/ranking?market=&sector=  랭킹 (ai_verdict/target_price 포함)
GET  /api/themes , /api/themes/{name}
GET  /api/anomalies
GET  /api/price/{code}          실시간 시세 (KIS→네이버 폴백)
GET  /api/candles/{code}?tf=    일/주/월봉
GET  /api/analyze/{code}        종합 분석 (핵심 엔드포인트, ai_verdict 포함)
GET  /api/kis/status , POST /api/kis/config   (공개모드 403)
GET  /api/ai/status , POST /api/ai/report/{code}   (공개모드 403)
POST /api/auth/signup|login|logout , GET /api/auth/me
POST /api/auth/change-password
GET  /api/admin/users           (관리자 전용)
GET  /api/push/key , POST /api/push/subscribe|unsubscribe , POST /api/push/test
GET  /api/watch , POST/DELETE /api/watch/{code}
GET  /api/portfolio , POST/DELETE /api/portfolio/{code}
GET  /sw.js                     서비스워커 (웹푸시용)
```

### 프론트엔드 (`static/`)

| 파일 | 내용 |
|---|---|
| `index.html` | **3-tab 구조**: `#landing`(홈) / `#report`(종목분석) / `#portfolio-view`(포트폴리오) + `#compare-view`/`#admin-view` + 모달들 |
| `app.js` | 전 로직. 상단 `#main-nav`가 3개 뷰 전환을 담당(`goHome()`/`analyze()`/`showPortfolio()`) |
| `style.css` | CSS 변수 기반 다크/라이트 테마 |

회원 DB(`users.db`)는 `STOCKLENS_DATA_DIR`(오라클: `/opt/stocklens-data`)에 저장 —
**코드 배포 경로(`/opt/stocklens`) 밖**이라 재배포가 회원 데이터를 안 건드린다.

---

## 3. 지금까지 구현된 기능 (전부 라이브 검증 완료, css v88 · js v97)

### 3.1 핵심 분석 엔진
- 6부문 점수화(가치평가18%·수익성20%·성장성22%·재무안정성12%·기술적추세16%·수급심리12%) → S~F 등급
- **AI 최종판단(5단계+확신도)**: `analysis.final_verdict()` — 종합점수를 매수(80+)/분할매수(65+)/
  보유(45+)/분할매도(30+)/매도 5단계로 변환. **확신도(50~95%)는 점수 재표시가 아니라, 6개 부문점수
  (+가능하면 밸류에이션 점수·컨센서스 상승여력)가 판단 방향과 얼마나 일치하는지를 잰 것** — 100%는
  과신 방지를 위해 절대 안 씀. 상세페이지(main.py)는 valuation까지 반영해 신호가 많고, 랭킹은
  무거운 재계산을 피해 total+cons만 사용.
- 밸류에이션 6항목 + `fair_buy_price()`(보수적/기준/낙관적 3단계 적정매수가) + 5단계 가격구간표
  (적극매수/매수/분할매수/관망/고평가) + **단계별 매수전략**(1차=optimistic/2차=base/3차=conservative,
  30/30/40% 배분)
- 이상징후 탐지(신호 3개 이상 겹칠 때만 플래그), 테마별 투자매력도 TOP10

### 3.2 3대 구조 개편 (Phase 1+2, 2026-08-14)
사용자가 "기능을 더 넣기보다 구조를 바꾸는 게 먼저"라며 전체 사이트를 **홈 / 종목분석 / 포트폴리오**
3화면 체계로 재구성 요청. Phase1(반드시)+Phase2(차별화) 완료, Phase3(알림 강화)는 대기 중.

- **① 네비게이션**: 상단 `#main-nav`(홈|종목분석|포트폴리오) — 기존 뷰 전환 함수 재사용.
- **② 홈 재배치**: Today's Pick(적정매수가·목표가·상승여력) → 왜 주목하는가(6개 부문점수+RSI
  과열경고, 신규계산 없음) → 오늘의 투자기회 TOP5 → 테마 → 관심종목(실시간가+AI판단 표시) →
  이상징후 → 전체랭킹. Today's Pick 1종목에 한해서만 `/api/analyze` 추가호출로 진짜 적정매수가를
  가져옴(랭킹 전체엔 안 넣음 — 무거워짐).
- **③ 종목상세 최상단**: 흩어져 있던 AI최종판단/한줄평/저평가문장/핵심지표/근거/가격별 투자전략을
  헤더 바로 아래 `#highlight-card` 하나로 응집(순수 HTML 이동, JS 로직 안 건드림). **탭 구조
  (종합/차트/밸류에이션/재무/수급/뉴스/리포트/AI분석) 전환은 의도적으로 범위 제외** — 리스크 큰
  대규모 변경이라 필요시 별도 요청으로 진행할 것.
- **④ 단계별 매수 전략**: 위 3.1 참고, 순수 프론트 렌더링.
- **⑤ AI 포트폴리오 진단 강화**: 포트폴리오 종합점수를 등급(S~F)으로 변환(`analysis.GRADE_TABLE`
  재사용), '오늘 하루 손익' 추가, 보유종목 테이블에 AI판단·권장비중 컬럼 추가.
- **⑥ AI 리밸런싱**: 아래 3.3 참고.

### 3.3 내 포트폴리오 (`app/portfolio.py`) — 이 앱의 핵심 차별화 기능
**국내+미국 종목을 함께 지원**(2026-08-18부터 — 실시간 환율 환산, 아래 3.6장 상세).

- 평가금액/비중/평가손익(평균단가 입력 시)/오늘 하루 손익/포트폴리오 등급(S~F)
- **변동성·최대낙폭**: 개별 종목 지표 가중평균이 아니라 보유종목 공통거래일 종가를 실제 합산해
  포트폴리오 가치 시계열을 재구성 후 계산(분산효과 반영된 진짜 숫자)
- **오늘의 진단**(`_today_actions`): 🔴비중과다(균등분산 대비 1.5배↑) / 🟡매수타이밍악화(전일대비
  점수 -8↓, `snapshot_score`/`snapshot_date` 컬럼으로 하루 1회만 갱신) / 🟢추가매수기회(적정매수가
  대비 -5%↓) / 🔴매도신호(목표가도달·RSI과열·외국인순매도전환 중 **2개 이상** 겹칠 때만)
- **위험 자동탐지**(`_risk_flags`): 종목쏠림(30%↑, 위험기여도%까지 공분산행렬로 계산)/업종쏠림
  (50%↑)/상관관계 클러스터(상관계수 0.7↑ 종목을 union-find로 묶어 합산비중 20%↑면 "사실상 같은 베팅")
- **실질 노출**(`_theme_exposure`): `themes.py` 큐레이션 재사용, ETF 룩스루는 데이터 소스 없어 의도적 제외
- **종목간 상관관계**: Pearson 상관계수 행렬(최근 1년 공통거래일 수익률)
- **AI 리밸런싱**(`_recommend_weights`): 종목별 AI판단(tier)에 배점(매수1.5/분할매수1.2/보유1.0/
  분할매도0.6/매도0.3)해 권장비중 계산, 종목당 상한 적용.
  ⚠️ **실제로 잡은 버그**: 상한을 고정 30%로 두면 종목 수가 적을 때 수학적으로 불가능한 조합이 나옴
  (3종목 중 2개가 상한에 걸리면 나머지 1개가 무조건 40%를 떠안는데, 그게 '매도' 등급이면 매도등급이
  최고비중을 받는 모순). **상한을 `max(30%, 100%/종목수×1.4)`로 동적 완화**하고 재분배도 반복(최대
  4회) 수렴시키도록 수정 — 되돌리지 말 것.
- **자동 알림**(`portfolio_alert.py`): 30분마다 보유종목 있는 전 사용자 재평가 → 매수 적정구간
  진입/매도 신호 시 웹푸시(`watch.send_to_user()` 재사용, 별도 옵트인 UI 불필요 — 관심종목 알림과
  같은 구독 사용). 같은(user,code,signal) 조합은 24시간 쿨다운.

### 3.4 회원/알림 인프라
- 아이디/비밀번호 인증(이메일 아님). **관리자 계정은 서버가 `admin`/`admin1234`로 자동 부트스트랩.**
- 웹푸시(VAPID): 관심종목 매수기회 알림(15분 주기) + 포트폴리오 매수/매도 신호(30분 주기)가 같은
  구독 인프라(`push_subs` 테이블, `watch.py` 소유이나 전역 공용)를 공유.

### 3.5 UI 버그·차별화 수정 (2026-08-14, v39/v40)
- **v39**: 종목 하나를 본 뒤 홈으로 돌아가면 `currentCode`가 null로 리셋되는데, 이 상태에서
  "종목분석" 탭을 누르면 `goHome()`이 조용히 재실행되며 "홈" 탭만 계속 활성화된 채로 남아
  버튼이 무반응처럼 보였다. 검색창에 펄스 하이라이트 애니메이션(`.search-wrap.pulse-hint`)을
  추가해 "먼저 검색하라"는 피드백을 명확히 줌.
- **v40**: 사용자가 "AI 투자기회 TOP5가 실시간 랭킹 상위 5개를 그대로 보여주는 것과 다를 게
  없다"고 지적 — 실제로 `renderTodayPick(d.items)`가 `/api/ranking`의 정렬 결과를 그대로
  `slice(0,5)`만 하고 있었다(진짜 중복). **`app.js`의 `pickOpportunities()`로 교체**: 종합점수
  상위 20개 후보군 안에서 극단적 과열(RSI 78+) 제외 후, 상승여력·RSI·PER 이력비·외국인수급·
  실적전망(`op_growth_fwd`, anomaly.py와 동일하게 50% 표시캡)을 반영한 `opportunityScore()`로
  재정렬. 각 종목에 왜 뽑혔는지 근거 칩(`opportunityReasons()`)도 표시해 차이가 눈에 보이게 함.
  **백엔드 추가 호출 없음** — `/api/ranking`이 이미 주는 필드만 프론트에서 재계산(랭킹 백그라운드
  루프에 무거운 계산을 얹지 말라는 CLAUDE.md 규칙 준수).
- **v41**: 사용자가 홈/종목상세/포트폴리오/헤더 전반에 걸친 UX 개편 12개 항목을 우선순위와 함께
  지시(2026-08-14). **1·2번만 이번에 완료, 3~12번은 다음 세션에 이어서 진행** — 상세는 아래
  "### 4.0 UX 대개편" 참고.

### 3.6 포트폴리오 미국 종목 지원 (2026-08-18, v48)
사용자 요청: "미국주식도 포트폴리오에 추가할 수 있게, 환율은 실시간으로 가져와서 반영. 어려우면
달러로만 표시해도 됨." — 실시간 환율 소스를 찾아 첫 번째 방식(완전 통합)으로 구현했다.

- **환율 소스**(`naver.usd_krw_rate()`): 네이버가 전용 환율 API를 안 준다(여러 후보 URL 다
  404/409로 시도해봄). 대신 `finance.naver.com/marketindex/` HTML을 정규식으로 파싱해
  `FX_USDKRW` 코드 옆 `<span class="value">` 값을 읽는다(하나은행 고시 기준, 실시간). 10분
  캐시(`naver._cache` 재사용). **실패하면 None**을 반환하고 절대 예외를 던지지 않는다.
- **폴백**: 환율 조회가 실패하면 그 요청에서 미국 보유종목만 `excluded`(사유: "환율 조회
  실패")로 빠지고 국내 종목은 정상 계산된다 — 포트폴리오 전체가 죽지 않는다. 다음 새로고침 때
  자동 재시도(캐시가 없으면 매번 재조회하므로 일시적 네트워크 문제면 곧 복구됨).
- **통화 분리 원칙**(`portfolio.py` compute() 핵심): 종목마다 `price_native`(달러/원 그대로)와
  `price`(원화 환산, 환율 곱함) 두 값을 같이 들고 다닌다.
  - `price_native`는 **analyze_fn()이 이미 계산해둔 판단 신호**(적정매수가·목표주가·RSI 등,
    전부 종목 통화 그대로)와 비교할 때만 쓴다 — `buy_discount_pct`, 매도신호의 목표가 도달
    판정이 여기 해당. 환산가와 섞어 비교하면 단위가 안 맞아 전부 틀린 판정이 나온다(이 실수를
    하지 않도록 코드에 주석으로 명시해둠).
  - `price`(환산가)는 **합산이 필요한 계산**(평가금액·비중·업종분산·상관관계·포트폴리오
    시계열)에만 쓴다.
  - `avg_price`(평균단가)는 사용자가 입력한 **종목 통화 그대로** 저장(달러는 달러로) — 원화로
    강제 변환 안 함, 사용자가 매수할 때 본 그 통화 그대로 보여줘야 직관적이라서.
- **변동성·상관관계용 시계열**(`price_by_date`)도 원화 환산해서 합산하는데, **현재 환율을
  과거 1년 전체에 균일 적용하는 근사치**다(과거 환율 이력 소스가 없음). 변동성(%)·상관계수는
  스케일 불변이라(전 구간에 같은 상수를 곱해도 수익률·상관계수는 안 변함) 이 근사가 계산
  자체를 왜곡하지는 않는다 — 다만 실제 과거 환율 변동분만큼은 반영이 안 된다는 것을 알고
  있을 것(정밀 백테스트 용도로는 못 씀, 현재 시점 리스크 진단용으로는 충분).
- **섹터/테마 매핑 확장**: `_SECTOR_MAP`이 `ranking.UNIVERSE`(국내)뿐 아니라
  `ranking.US_UNIVERSE`(미국 190종목)도 합쳐서 참조하도록 변경 — 미국 종목도 업종 쏠림 경고
  대상이 됨. `_theme_exposure`도 국내(`market=="KR"`)로만 필터하던 걸 풀어서 `themes.py`에
  이미 있던 미국 코드(예: NVDA.O, AAPL.O)까지 매칭하도록 함(themes.py는 원래부터 미국 코드를
  갖고 있었음 — 포트폴리오가 국내만 지원해서 안 쓰고 있었을 뿐).
- **프론트**(`static/app.js`): 포트폴리오 검색을 `market=KR` 필터 없이(국내+미국 모두) 검색하도록
  변경, 검색 결과에 🇺🇸/🇰🇷 국기 표시. 종목 선택 시 평균단가 입력창 placeholder가 "평균단가(달러,
  선택)"로 자동 전환. 보유종목 표는 평균단가·현재가를 종목 통화 그대로(`pw(v, currency)`)
  보여주고, 평가금액·평가손익은 원화 환산값에 "(환산)" 표시를 붙임. 요약카드 하단에
  "환율: $1 = ₩1,411.50 기준으로 환산됨" 안내문 추가.
- **백엔드 신규 API 없음** — 기존 `/api/portfolio` 응답에 필드(`currency`/`price_native`/
  `fx_rate`)만 추가.

### 3.7 모바일 레이아웃 버그 수정 (2026-08-19, v50)
사용자가 "휴대폰으로 볼 때 UX/UI가 흐트러지는 게 꽤 있다"고 지적. `resize_window`(모바일
프리셋)+DOM 좌표 비교로 겹침·오버플로 전수 조사 후 실제로 확인된 것만 수정:

- ⚠️ **`.pf-search-wrap` 260px 높이 버그** (사용자가 정확히 짚어낸 그 증상 — "종목추가에서
  수량/평균단가가 왜 이렇게 떨어져 있냐"): `.pf-search-wrap { flex: 1 1 260px; }`는 데스크톱
  가로 레이아웃에서 "너비 260px"를 의미했는데, 모바일에서 `.pf-add-row`가
  `flex-direction: column`으로 바뀌면 flex-basis의 의미가 **세로축(높이)** 로 바뀌어
  검색창이 260px 높이의 텅 빈 박스가 되고 그 아래 수량·평균단가·추가 버튼이 전부 밀려났음.
  `flex-direction: column`으로 바뀌는 모바일 미디어쿼리에서 `flex: none`도 같이 리셋하도록
  수정 — **flex-basis가 px로 고정된 요소를 담은 컨테이너를 모바일에서 column으로 바꿀 때는
  항상 flex-basis도 같이 리셋할 것(반복될 수 있는 패턴, 다른 `flex: N N Npx` 요소 추가 시
  주의)**.
- **헤더 4행→3행 압축**: `⚙ 실시간 시세 연동`/`⚡ 실시간 시세 연결됨` 버튼 라벨을
  `<span class="kis-label">`로 감싸고 720px 이하에서 `display:none` — 아이콘만 남아
  logo·테마토글·로그인 버튼과 함께 1행에 들어감(`title` 속성으로 접근성 유지). 비로그인
  방문자 기준 헤더 높이 204px→159px. 로그인 상태(버튼 여러 개)는 여전히 넉넉히 wrap됨.
- **`.mypf-cta` 버튼 폭**: 모바일에서 문단+버튼이 세로로 쌓일 때 버튼이 콘텐츠 폭(160px)만큼만
  차지해 어중간했음 → `flex-direction: column` + `width:100%`로 꽉 채움.
- **조사했지만 실제 버그가 아니었던 것**: `getBoundingClientRect()`로 겹침을 스캔하면 여러 줄로
  감싸지는 inline 요소(예: `.hint` 설명문)가 "가짜 겹침"으로 잡힌다 — 그 요소의 bounding box가
  모든 줄을 감싸는 사각형이라 실제로 안 겹쳐도 겹친 것처럼 보임. `getClientRects()`(줄 단위)로
  재검증해야 정확함 — 다음에 비슷한 조사할 때 이 오탐 패턴 기억할 것.
- 가로 스크롤(페이지 레벨)은 어디에도 없었음 — `.table-wrap`/`.price-ladder` 등은 원래부터
  자체 `overflow-x:auto`로 격리되어 있어 정상.

### 3.8 데이터 정확도 감사 — 미국 종목 재무 (2026-08-19)
사용자가 "데이터 정확도, 특히 미국주식 이익 같은 것들"을 재확인해달라고 요청. AAPL/TSLA/NVDA
raw finance API 응답을 실제 공시 수치(매출액·영업이익·당기순이익)와 직접 대조해 **단위 환산
로직 자체(analysis.py `_highlight_rows`의 ÷100, valuation.py의 EV/EBITDA ÷100)는 전부
정확함을 확인**(NVDA FY2025 매출 1304.97억달러=$130.497B, 당기순이익 728.8억달러=$72.88B —
둘 다 실제 공시치와 사실상 정확히 일치). 다만 그 과정에서 실제 버그 2건을 발견해 수정:

- ⚠️ **재무 하이라이트 표 통화 라벨 오표기(핵심)**: `static/index.html`의
  `#finance-unit-hint`가 "단위: 억원"으로 고정돼 있었음. 값 자체는 미국 종목도 정확히
  "억 달러" 스케일로 환산되고 있었는데, 라벨만 항상 "억원"이라고 표시 — 예를 들어 AAPL
  매출액 "4,162"를 사용자가 "4,162억원"(약 $290M)으로 오인할 수 있었음(실제는 "4,162억
  달러" = $416.2B, **약 1400배 차이**). `renderFinance()`에서 `curCur`(이미 `render()`
  최상단에서 `d.currency`로 설정됨) 기준으로 "단위: 억 달러" ↔ "단위: 억원"을 동적으로
  전환하도록 수정. 사이트의 다른 곳들(시가총액 `m938`, 동종업계 시총 `m983`)은 이미
  `curCur` 기준으로 올바르게 되어 있었고 이 표 하나만 예외였음.
- ⚠️ **저기반 성장률 왜곡값이 캡 없이 그대로 노출**: `op_growth_fwd`(컨센서스 영업이익
  전망 성장률)가 적자→흑자 등 저기반 회복 시 수백%로 튀는 문제는 이미 알려진 함정
  (`anomaly.py GROWTH_DISPLAY_CAP`, `valuation.py PEG_GROWTH_CAP`, 둘 다 50%)인데,
  **`valuation.py`의 "실적방향" 신호/체크리스트 문장**과 **`analysis.py`의
  `build_opinion()` 성장성 서술 문장**은 이 캡을 안 걸고 원본을 그대로 문장에 넣고
  있었음(실측: 삼성전자 "컨센서스 영업이익 전망 +796.6%"가 종목상세 "밸류에이션 분석"
  신호와 "부문별 평가" 서술문에 그대로 노출됨 — 점수 계산은 이미 상한이 걸려있었는데
  **사람이 읽는 문장만 캡이 안 걸려있던** 불일치). `analysis.py`에 `GROWTH_DISPLAY_CAP =
  50.0` 상수를 새로 추가하고, 두 곳 모두 표시값을 이 상수로 캡 + 캡이 걸렸을 때
  "(저기반 효과로 상한 표시)" 문구를 붙이도록 수정. `ai.py`의 Claude 심층리포트 프롬프트는
  (AI가 맥락 판단할 정보라 원본값 자체는 유지하되) 극단값(|x|>100%)이면 "액면 그대로
  강조하지 말 것"이라는 주의 문구를 붙임.
- **교훈**: 저기반 왜곡 방지 캡은 "점수 계산"과 "사람이 읽는 문장" 양쪽에 **항상 같이**
  적용해야 한다 — 점수만 캡하고 텍스트를 캡 안 하면(또는 반대) 점수와 설명이 서로 다른
  숫자를 말하는 모순이 생긴다. 새로 `{value:.0f}%` 같은 서술 문장을 추가할 때마다 그
  값이 저기반 왜곡 가능한 필드(`op_growth_fwd`, `rev_growth_fwd` 등)인지 확인할 것.

### 3.9 종목상세 탭 구조화 (2026-08-19, v52) — ⑦번, UX 개편 12개 항목 전부 완료
사용자: "종목분석 뭐가 너무 많아. 깔끔하게 만들어봐. 자료가 너무 방대해." — Phase1부터
의도적으로 범위 제외해뒀던(리스크 크다고 판단) 바로 그 작업. 종목상세의 카드 10개를
7개 탭으로 묶었다: **[종합][차트][밸류][재무][수급][뉴스·리포트][AI분석]**.

- **항상 보이는 것(탭 밖)**: 헤더(`.stock-header`), AI 최종판단 히어로(`#highlight-card`).
  이 둘은 "5초 요약"의 핵심이라 탭 뒤에 숨기지 않음.
- **탭별 매핑**: 종합=부문별평가+목표주가&진입타이밍 / 차트=`#chart-card`(캔들차트+기술지표
  +고급차트/미너비니, 원래도 한 카드였음) / 밸류=`#val-card`+투자지표+동일업종비교(피어
  PER비교는 밸류에이션 개념이라 묶음) / 재무=재무하이라이트 / 수급=투자자수급동향(국내
  전용) / 뉴스·리포트=증권사리포트+뉴스&시장심리 / AI분석=`#ai-card`.
- **구현**: 각 `<section class="card">`에 `data-tab="X"` 속성만 추가(내부 재구조화 없음,
  최소 침습). `showDetailTab(tab)`이 `#detail-tabs button`의 active 클래스와 카드의
  `.tab-hide`(신규 CSS, `display:none`)를 토글. 종목이 바뀔 때(`render()` 시작부)마다
  `showDetailTab("overview")`로 리셋. 수급 탭 버튼은 `d.flows`가 없으면(미국) 숨김,
  AI분석 탭 버튼은 `!d.ai_enabled`면 숨김(공개배포에선 거의 항상 숨겨짐) — 기존에 카드
  자체를 숨기던 로직은 그대로 두고 탭 버튼 숨김만 나란히 추가.
- ⚠️ **실제로 잡은 버그 1: 탭 버튼 자체가 사라짐**. `showDetailTab()`이 처음에
  `#report [data-tab]`(후손 전체)로 토글했는데, 탭 버튼(`<button data-tab="chart">`
  등)도 `#detail-tabs`를 거쳐 `#report`의 후손이라 **같이 걸려서 비활성 탭 버튼 6개가
  전부 사라지는** 버그였음(종합 탭 버튼만 남고 나머지 탭으로 이동할 방법이 없어짐).
  `#report > [data-tab]`(직계 자식만)으로 바꿔 해결 — 카드는 `#report`의 직계 자식,
  탭 버튼은 `#detail-tabs`의 자식(손자)이라 이걸로 정확히 분리됨. **이 shorthand
  패턴(`data-tab`을 컨테이너와 그 안의 네비 둘 다에 쓰는 것)을 또 쓸 일이 있으면
  꼭 직계 자식 선택자로 스코프를 좁힐 것.**
- ⚠️ **실제로 확인한 위험 2: 숨겨진 상태에서 그려지는 캔버스**. `#chart-card`의 메인
  캔들차트는 `lightweight-charts`의 `autoSize:true`(내부적으로 ResizeObserver 사용)라
  `display:none`으로 생성된 뒤 나중에 보여져도 **스스로 올바른 크기로 재조정됨**(실측
  확인 — 탭 전환 후 캔버스가 690×515 같은 정상 크기로 나옴, 별도 코드 불필요). 반면
  `#finance-chart`(일반 2D canvas, `renderFinance()`가 `c.width =
  parentElement.clientWidth`로 계산)는 **자가복구가 안 됨** — 숨겨진 상태에서 그려지면
  폭이 음수/0이 되어 깨진다. `showDetailTab()`이 "재무" 탭으로 전환될 때
  `renderFinance(lastAnalysis.finance_rows)`를 다시 호출해 해결(재계산 비용 저렴,
  네트워크 호출 없음). **교훈: `clientWidth`/`offsetWidth`로 크기를 재는 캔버스는
  탭/아코디언 등으로 숨겨질 수 있는 곳에 있으면 반드시 "보여질 때 다시 그리기"가
  필요하고, `autoSize`류 라이브러리 캔버스는 대체로 스스로 처리하지만 실측으로
  확인하고 넘어갈 것(가정하지 말 것).**
- 검증: 로컬에서 KR(005930)·US(AAPL.O) 양쪽 모두 7개 탭 전부 클릭, 캔들차트·재무차트
  픽셀 데이터까지 확인(빈 캔버스가 아님을 직접 확인), 모바일 375px에서 탭바가 가로
  스크롤 없이 한 줄에 다 들어가는 것까지 확인.

### 3.10 캔들차트 개선 (2026-08-19, v53)
사용자: "차트도 개선해봐 너무 후져." 메인 캔들차트(`lightweight-charts` v4)를 점검해
두 가지를 바꿈:

- **OHLC 크로스헤어 정보박스 추가**(`.chart-ohlc-legend`, 차트 좌상단 오버레이): 값 없이
  색점+범례 텍스트만 있던 것에서, TradingView류 차트의 표준 UX인 "지금 이 캔들이
  얼마였는지"(시/고/저/종가·전일대비 등락%·거래량)를 보여주는 박스로 교체. 기본은
  최신 봉을 보여주고, `chart.subscribeCrosshairMove()`로 마우스를 올린 시점의 값으로
  갱신. 국내/미국 통화(`pw()`)에 맞춰 원화/달러 자동 전환.
  ⚠️ **실제로 잡은 버그**: lightweight-charts v4의 `seriesData.get(series)`가 주는 값
  객체엔 `time`이 없다(콜백의 `param.time`에 별도로 있음) — `bar.time`으로 전일종가
  조회 키를 삼았더니 항상 `undefined`라 등락% 표시가 안 뜨는 버그였음. `time`을 별도
  인자로 명시적으로 넘기도록 수정(`updateOhlcLegend(time, bar, vol)`).
- **낡은 커스텀 구간슬라이더 완전 제거**: `#chart-container` 아래 있던 수동 구현 듀얼
  핸들 슬라이더(`range-slider`/`rs-*`, 직접 드래그 이벤트 처리)를 삭제. 차트 자체가
  이미 `handleScroll.pressedMouseMove`(드래그 이동)·`handleScale`(휠/핀치 확대축소)로
  네이티브 팬/줌을 지원하고 기간 버튼(3개월~전체)도 있어서, 손으로 만든 슬라이더가
  기능이 겹치면서 오래된 UI처럼 보이게 하는 요인이었음. 관련 JS 전부(`rsState`,
  `rsSync`, `paintSlider`, `applySliderToChart`, `initRangeSlider`) 및 CSS 삭제.
- 검증: 로컬에서 KR·US 종목 기본 OHLC 박스 렌더링(실제 값 확인), 일봉/주봉/월봉 전환 시
  갱신, 모바일 375px에서 박스가 `.chart-wrap` 밖으로 안 나가는 것 확인. 실제 마우스
  호버 인터랙션은 이 환경(스크린샷 항상 타임아웃)에서 검증 불가 — `subscribeCrosshairMove`
  API 자체는 공식 문서 패턴 그대로라 신뢰도 높음, 추후 실제 브라우저에서 확인 권장.

### 3.11 외부 진단 리포트 반영 — P0 신뢰도 결함 (2026-08-19, v54)
사용자가 Claude Opus로 검증한 `StockLens_진단리포트_3.docx`를 전달, "SK하이닉스 재무재표는
맞게 된 것 같은데 너가 한번 더 확인해봐"라고 재검증을 요청. 리포트의 P0(신뢰 붕괴 방지)
7개 항목 중 실행 가능한 것 + 검증 중 발견한 추가 결함을 처리:

- **⚠️ 실제로 확인됨 — 컨센서스 추정치 이상치**: SK하이닉스 2026 컨센서스가 매출
  345.8조(2025 대비 3.6배)·영업이익률 77.1%로, 네이버 원본 데이터 자체가 이 값이었음
  (파싱 버그 아님 — 소수 애널리스트 추정치 단순평균에 이상값 하나가 섞여 평균을
  오염시킨 것으로 추정). 이 값이 선행PER 4.4배→PEG 0.09→"저평가"로 그대로 전파돼
  종합점수·랭킹까지 부풀리고 있었음. `app/analysis.py`의 `fundamental_analysis()`에
  검증 레이어를 추가: 추정 영업이익률>60%, 추정 순이익률>50%, 매출 전망>100% 중 하나라도
  걸리면 `consensus_flagged=True`로 표시하고 `cns_per`/`op_growth_fwd`/`rev_growth_fwd`를
  점수·밸류에이션 계산에서 제외(값 자체는 `*_raw` 필드로 보존해 화면엔 계속 노출).
  `app/valuation.py`(6순위 실적방향·PEG·1순위 FPER 밴드가 자동으로 안전한 값으로
  폴백)·`app/ai.py`(AI 리포트 프롬프트에 "검증 보류" 주의문구)·`static/app.js`
  (재무 탭에 `.cns-warn` 경고 배지)·`app/analysis.py`의 `build_opinion()`(부문별 평가에
  경고 문장) 전부 연동. 검증: 로컬에서 SK하이닉스 재분석 시 밸류 점수 저평가→"적정 수준"으로
  정상화, 종합점수 85→83.3점(A등급 유지, 실제 ROE 44%·실적 성장은 진짜라 A등급 자체는
  타당함).
- **⚠️ 실제로 확인됨 — 동종업계 PER 비교에 자기 자신 포함**: `main.py`가 편의상
  `peers_per`에 분석 대상 종목 자신을 `self:True`로 끼워 넣는데, `app/valuation.py`의
  `peer_comparison()`이 이를 걸러내지 않아 비교군 중앙값이 항상 자기 자신 쪽으로
  끌려가 "14.72배 vs 14.72배" 같은 무의미한 "업종 평균 수준" 결론만 나오고 있었음
  (실측 확인). 계산(`vals`)·표시 목록(`peers`) 양쪽에서 `not p.get("self")` 필터 추가.
- **차트 Y축 음수 구간**: `createPriceLine()`으로 그린 목표주가선이 기본적으로
  자동스케일에 포함돼, 목표주가가 현재가보다 훨씬 높을 때(하락장) Y축이 음수까지
  늘어나며 캔들이 화면 위 15%에 눌리는 버그. `candleSeries.applyOptions({
  autoscaleInfoProvider })`로 자동스케일을 "보이는 구간의 캔들 고가·저가"만으로
  직접 계산하도록 교체.
  ⚠️ **실제로 잡은 버그**: 처음 구현에서 이 콜백 안에서 `chart.timeScale()
  .getVisibleLogicalRange()`를 직접(동기) 호출했더니 — 가격축 계산 중 시간축 상태를
  조회하는 재진입이 발생해 `setVisibleLogicalRange()`로 건 기간 필터가 그 즉시
  원래대로 되돌아가는 버그가 생겼다(기간 버튼이 완전 먹통). `subscribeVisibleLogicalRangeChange()`로
  마지막 통지 구간만 별도 변수에 캐싱하고 그 캐시만 읽도록 수정해 재진입을 차단.
- **기간 필터 동작 안 함**: `showDetailTab("overview")`가 종목 재분석마다 기본 실행되며
  차트 탭(`data-tab="chart"`)이 `display:none`인 상태에서 `drawChart()`가 먼저 실행되고
  있었음 — 컨테이너 width=0일 때 건 `setVisibleLogicalRange()`가 이후 탭이 보이면서
  ResizeObserver가 재계산할 때 무시되고 전체 구간으로 되돌아가는 게 원인으로 추정.
  재무 탭과 동일한 패턴으로 `showDetailTab()`에서 "차트" 탭이 활성화될 때 `drawChart()`를
  다시 호출하도록 수정.
  ⚠️ **검증 한계**: 이 환경(자동화 브라우저)에서 `setVisibleLogicalRange()` 자체가
  완전히 빈 `<div>`에 최소 옵션으로 만든 차트에서도 호출 후 `getVisibleLogicalRange()`가
  전혀 반영이 안 되는 게 확인됨(우리 코드와 무관한 환경 한계 — 헤드리스 렌더러가 실제
  레이아웃/페인트 패스를 안 밟는 것으로 추정) → 이 상호작용은 실제 브라우저에서
  최종 확인 필요. 다만 재진입 버그는 원인이 명확해 수정이 타당하고, "탭 숨김 상태에서
  그리기" 원인 진단도 근거가 있음.
- **단계별 매수가 역순 문제**: 1차·2차·3차 매수가(안전마진 20%/10%/0%)는 가격 순서상
  정상(낮을수록 안전마진↑)이지만, 주가가 이미 크게 빠진 상태에서는 1차·2차 가격이
  현재가보다 비싸 보여 "1차는 지금보다 비싸게 사라"로 읽히는 문제. 가격 로직은
  건드리지 않고(정상 동작이므로) `renderBuyPlan()`에 현재가 대비 도달 여부를 계산해
  ✅도달(즉시 집행 가능)/⏳대기(몇 % 더 하락해야 하는지)를 단계별로 표시, 이미 도달한
  비중 합계를 상단 배너로 안내하도록 변경.
- **가격 구간 바 라벨 겹침**: `renderPriceLadder()`가 가격이 근접한 지점들의 라벨을
  겹쳐 그리던 문제 — 트랙 최소폭(480px) 기준으로 라벨 폭(88px)보다 가까운 지점을
  감지해 세로로 30px씩 어긋나게 쌓도록 수정(`margin-top` 단계 부여, 트랙 높이도
  단계 수만큼 동적 확장).
- **판단 문구 일원화**: `ai_verdict`(펀더멘털·수급 종합)와 `tech.verdict`(단기 차트
  진입타이밍)가 서로 다른 질문에 답하는 별개 지표인데 라벨 없이 나란히 노출돼 모순처럼
  읽히는 문제 — 점수 로직 자체를 합치는 건 리스크가 커서(두 지표가 실제로 다른 걸
  측정하므로 강제 일치시키면 오히려 부정확) 대신 각 배너에 "무엇을 살까"/"언제 살까"
  구분 설명을 추가. 차트 탭 "기술 점수"(단기 지표만)와 종합 탭 "기술적추세"(단기+
  `chart_pro` 장기구조 50:50 블렌드, 실측 확인: 39점 vs 58점처럼 다를 수 있음)도
  이름이 같아 보여 혼란을 주던 것 → 라벨을 "단기 기술점수"로 바꾸고 툴팁으로 설명.
- **테마 탭 빈 상태**: `loadThemeChips()`가 칩만 그리고 기본 선택이 없어 첫 진입 시
  빈 화면이던 것 → 첫 테마를 자동 선택하도록 수정.
- **모바일 TOP5 표 헤더/본문 컬럼 불일치**: 모바일 CSS가 데이터 셀(`.today-score`,
  `.today-upside`)만 숨기고 헤더의 대응 `<span>`은 숨기지 않아, 헤더는 5개 라벨인데
  본문은 3열만 보이는 불일치(실측 확인). 헤더·본문 공통 `.today-hide-mobile` 클래스로
  통일.
- **한글 줄바꿈**: 공백 없는 한글 단어(예: "당신이")가 임의 글자 사이에서 줄바꿈되는
  문제(모바일 히어로 카피 "결정은 당신 / 이."로 끊김) → `body`에 전역
  `word-break: keep-all` 적용.
- 검증: 로컬 서버에서 SK하이닉스·삼성전자 재분석 API 응답 직접 확인(컨센서스 플래그·
  동종업계 비교·밸류 점수 정상화), 브라우저에서 DOM 조사로 가격사다리 스태거링·단계별
  매수 도달상태·테마 기본선택·모바일 375px 헤더-본문 컬럼 일치·`word-break: keep-all`
  적용 전부 실제 확인. 콘솔 에러 없음.
- **P0 중 미착수(3.12에서 처리)**: URL 라우팅. 리포트의 P1(백테스트 대시보드·스크리너·
  이메일 가입·법적 문서·정식 도메인)·P2(커버리지 확대·OG 이미지 등)는 여전히 미착수.

### 3.12 URL 라우팅 도입 (2026-08-19, v55) — 진단리포트 P0 마지막 항목
사용자 지시: "URL 라우팅부터 이어서 착수해줘". 이전까지는 어느 종목을 열어도 주소창이
항상 `/` 그대로라 종목 페이지를 북마크·공유할 수 없고, 브라우저 뒤로가기도 안 먹혀
자체 "← 랭킹·검색으로 돌아가기" 버튼으로 대신하고 있었다(진단리포트 §7-1). 프레임워크
없이(바닐라 JS 유지 원칙) History API만으로 구현:

- **백엔드**(`main.py`): `/stock/{code}` 라우트 추가, `/`와 동일하게 `index.html`을 그대로
  내려준다. 실제 라우팅 로직은 전부 클라이언트에 있다 — 서버는 "이 경로도 SPA 진입점"이라는
  것만 알면 된다.
- **프런트**(`static/app.js`): `analyze(code, fromPopstate)`가 성공 시
  `history.pushState({code}, "", "/stock/{code}")`로 주소창을 갱신하고 `document.title`도
  `"{종목명} — StockLens"`로 바꾼다. `goHome(fromPopstate)`는 `"/"`로 pushState. 두 함수 모두
  `fromPopstate` 인자로 "브라우저 뒤로/앞으로가기가 이미 주소를 바꿔놓은 상태에서 불렸는지"를
  구분해 이 경우엔 pushState를 또 하지 않는다(안 그러면 뒤로가기 한 번에 히스토리 스택이
  꼬여 계속 제자리로 돌아오는 버그가 생김). 전역 `popstate` 리스너가 `location.pathname`을
  다시 읽어 `/stock/{code}`면 `analyze(code, true)`, 아니면 `goHome(true)`를 호출해 화면을
  주소와 동기화한다. 스크립트 맨 아래에서 최초 진입 URL도 같은 패턴으로 검사해, 공유된
  링크·북마크·새로고침으로 `/stock/000660`에 바로 들어와도 그 종목이 자동으로 열린다.
- ⚠️ **실제로 잡은 버그 1**: `popstate` 핸들러가 `analyze()`/`goHome()`을 부를 때 포트폴리오·
  비교·관리자 화면이 열려 있어도 그걸 안 닫는다는 걸 놓칠 뻔했음 — `analyze()`는 원래
  랭킹·홈에서 종목을 클릭할 때만 쓰이던 함수라 그 화면들을 닫을 필요가 없었는데, 이제는
  뒤로가기로도 호출되므로 그 화면들이 열린 채로 종목 상세와 겹쳐 보일 수 있다. `popstate`
  리스너 안에서 세 화면을 먼저 강제로 숨기도록 추가.
  ⚠️ **실제로 잡은 버그 2**: 실패 처리(존재하지 않는 코드로 `/stock/XXX` 직행)에서 처음엔
  주소를 `"/"`로 되돌리는 조건에 `!fromPopstate`를 걸었는데, **최초 진입 시의 자동 분석
  호출 자체가 `fromPopstate=true`로 넘어가고 있어서**(주소창이 이미 그 URL이니 pushState는
  건너뛰어야 한다는 로직을 그대로 재사용했기 때문) 실패 시 URL 복구 코드가 항상 건너뛰어져
  죽은 링크가 주소창에 그대로 남는 버그로 실제 나타남(로컬에서 `/stock/NOTREAL` 직행으로
  재현·확인). "새 히스토리를 안 쌓는다"와 "URL을 고친다"는 서로 다른 결정이라 조건을 분리:
  실패 시엔 `fromPopstate`와 무관하게 항상 `history.replaceState(null, "", "/")`로 되돌리되,
  `pushState`가 아니라 `replaceState`를 써서 새 스택 항목을 만들지 않는다(뒤로가기로 들어와
  실패한 경우에도 스택이 꼬이지 않게).
- 검증: 로컬에서 `/stock/000660` 직행 진입(자동 분석+제목 반영), 홈→종목→홈→다른 종목
  이동 후 브라우저 `back()`을 두 번 눌러 각 단계의 URL·화면·제목이 정확히 일치하는지,
  `forward()`로 다시 돌아오는지, 존재하지 않는 코드(`/stock/NOTREAL`) 직행 시 홈으로
  정상 폴백하고 주소도 `/`로 복구되는지까지 전부 브라우저에서 실제 실행해 확인. 콘솔
  에러 없음(기존에도 있던 무관한 "unknown error fetching script" 노이즈 제외).
- **범위**: 종목 상세 페이지만 라우팅(리포트가 지목한 핵심 사례 — "이 종목 분석 링크
  보내줄게"). 포트폴리오·비교·관리자 화면은 로그인 종속·임시 상태라 라우팅 대상에서
  제외. 서버사이드 `<title>`/OG 메타 태그(리포트 §7-2, 크롤러·공유 미리보기용)는 별도
  항목이라 이번엔 손대지 않음 — 클라이언트 `document.title`만 바꿔서 브라우저 탭·북마크
  이름은 개선되지만, 공유 링크 미리보기에는 아직 반영 안 됨.

### 3.13 진단리포트 P1 착수 (2026-08-19, v57)
사용자 지시: "P1 이어서 진행해줘". P1 8개 항목 중 도메인 구매·소셜로그인(카카오/네이버
앱 등록)·법적 문서(사업자 정보)·유사투자자문업 신고 여부는 결제·실제 사업자 정보·법률
자문이 필요해 대신 결정할 수 없다고 안내하고, 순수 코드로 되는 4개 항목을 구현:

- **조건 조합 스크리너(`app/screener.py`, 신규)** — "유료 전환 1순위 기능"(리포트).
  `app/ranking.py`가 30분마다 백그라운드로 전종목(국내181+미국190) 채점하며 캐시해둔
  지표를 그대로 필터링만 한다(추가 네트워크 호출 없음, CLAUDE.md의 "무거운 집계는
  백그라운드+캐시" 원칙). `_score()` 반환값에 `per`·`pbr`·`roe`·`debt_ratio`·
  `dividend_yield`·`market_cap`·`market`을 추가로 실어보내는 것이 전부라 랭킹 자체의
  계산 비용은 그대로다. `/api/screener` — 시장·섹터·등급하한·종합점수·PER·PBR·ROE·
  부채비율·배당수익률·목표가 상승여력·외국인 5일 연속 순매수(리포트가 예시로 든 정확한
  조합) 등을 조합 필터링. 헤더 상단에 "🔎 스크리너" nav 항목 신설.
  ⚠️ **실제로 잡은 버그**: `analyze()`는 원래 랭킹·홈에서 종목을 클릭할 때만 쓰이던
  함수라 자신을 호출한 화면(포트폴리오·비교·관리자·이제 스크리너까지)을 안 닫았다.
  스크리너 결과 행을 클릭해 종목 상세로 이동하면 스크리너 화면이 뒤에 계속 깔려 있는
  게 실제로 재현됨(브라우저에서 직접 클릭해 확인). `analyze()` 최상단에서 4개 화면을
  전부 닫도록 통일해 근본 수정(이 화면들이 앞으로 어디서 호출되든 안전).
- **점수 백테스트 대시보드(`app/backtest.py`, 신규)** — 리포트 9장이 "유료화의 결정적
  근거"로 지목한 항목. ⚠️ **과거 데이터를 흉내 내지 않는다**: 현재 시스템은 재무제표·
  컨센서스·기술지표를 전부 "현재 값"만 제공받아, 과거 시점 기준으로 점수를 재구성할
  방법이 없다 — 그런 상태에서 "과거 백테스트"를 만들면 사실상 조작이다. 대신 오늘부터
  `ranking.py`의 매 계산 주기(30분)마다 `backtest.snapshot()`을 불러 그 날의 전종목
  {점수·등급·종가}를 `data/backtest_snapshots.jsonl`에 append한다(하루 1회만 실제
  기록되도록 `data/backtest_state.json`으로 시장별 마지막 기록일을 추적해 idempotent
  처리). `/api/backtest` — 1주/1개월/3개월/6개월/12개월 시점의 스냅샷을 찾아 그때 S/A/
  B/C이하 등급이었던 종목들이 "오늘까지" 실현한 평균수익률·승률·코스피(국내)·SPY(미국)
  대비 초과수익률을 계산. 데이터가 없는 기간은 "🔒 아직 데이터 부족(N/M일째)"로 솔직하게
  표시 — 오늘 막 시작했으니 1주일 뒤부터 첫 결과가 뜬다. 홈 화면에 "📊 이 점수, 실제로
  통했을까?" 섹션으로 상시 노출(로그인 불필요, 리포트가 요구한 "누구나 볼 수 있게").
- **뉴스 관련성 필터(`app/analysis.py news_sentiment()`)** — 리포트 실측 사례: SK하이닉스
  뉴스탭에 "카카오페이증권 주식 축의금 캠페인"·"한투운용 라이브 세미나"처럼 무관한
  기사가 섞여 "시장 심리 긍정적 70점"의 근거로 쓰이고 있었음. `news_sentiment()`에
  `stock_name` 파라미터를 추가해 제목+본문에 종목명이 등장하는 기사만 심리 점수 집계에
  반영(무관한 기사는 화면에서 숨기지 않고 "관련성 낮음" 배지만 붙임 — 이 프로젝트의
  일관된 원칙: 값을 숨기지 말고 정직하게 보여주되 점수 계산에서만 제외). 실측 확인:
  SK하이닉스 뉴스 3건 중 2건이 실제로 무관 판정돼 제외되자 심리 점수가 바뀜.
  원문 링크(`n.url`)는 사실 백엔드(`naver.py news()`)에 이미 있었고 프런트도 이미
  `<a href>`로 감싸고 있었다 — 리포트가 "링크가 없다"고 본 건 URL이 비어 있는 개별
  기사 항목이었을 가능성이 높음(Naver가 일부 프로모션성 콘텐츠엔 `mobileNewsUrl`을
  안 채워줌). `n.url`이 빈 경우 죽은 링크(`href=""`)를 만들지 않고 일반 텍스트로
  대체하도록 방어 코드 추가, `rel="noopener noreferrer"`도 함께 붙임.
- **모바일 헤더 스크롤 축소** — 리포트 지적사항(고정헤더 3줄이 화면의 22% 항상 차지).
  720px 이하에서만 아래로 스크롤하면 헤더가 위로 슬라이드아웃되고, 위로 스크롤하면
  다시 나타난다(`.topbar { transition: transform }` + `.topbar-hide { translateY(-100%) }`,
  `app.js`의 scroll 리스너). 데스크톱은 전혀 손대지 않음.
  ⚠️ **검증 한계**: 이 자동화 브라우저 환경은 `window.scrollTo()`가 실제로 스크롤을
  일으키지 않는다(이 프로젝트에서 이미 알려진 한계 — computer 도구의 좌표 클릭/호버가
  안 되는 것과 같은 부류). `scrollY`를 모킹해 리스너와 동일한 로직만 직접 실행해
  로직 자체(아래로 80px 초과 스크롤 시 숨김, 위로 스크롤 시 다시 표시)는 검증했지만,
  실제 터치 스크롤 동작은 실기기에서 확인 필요.
- **미착수(사용자 결정 필요)**: 도메인 구매, 카카오/네이버/애플 소셜 로그인(각 사
  개발자 콘솔에 앱 등록 후 client id/secret 필요), 이메일 필수 수집(가능은 하나 소셜
  로그인과 묶어서 한 번에 하는 게 나을지 확인 필요), 이용약관·개인정보처리방침·사업자
  정보(실제 사업자등록번호 등 필요), 유사투자자문업 신고 여부(법률 자문 사안).
- 검증: 로컬에서 `/api/screener`(ROE≥15%·PER≤15+등급B 이상 조합) curl로 실제 필터링
  결과 확인, 스크리너 화면 클릭→종목상세 이동 시 화면 겹침 버그 재현 후 수정 확인,
  `/api/backtest` 빈 상태 확인, `/api/analyze/000660` 뉴스 3건 중 실제 관련성 판정과
  심리 점수 변화 확인, 헤더 축소 로직 단위 검증. 콘솔 에러 없음(기존 무관 노이즈 제외).

### 3.14 스크리너 모바일 버그 수정 + 홈 화면 개편 (2026-08-19, v59)
사용자: "스크리너가 폰으로 보면 UI,UX가 이상해. 제대로 다시 만들어줘. 그리고 홈페이지
맨 앞에 삼성전자, sk하이닉스 이런거 다 지우고 그냥 관심종목을 맨 위로 올려."

- ⚠️ **실제로 잡은 버그 — 스크리너 폼 모바일 그리드 붕괴**: 375px에서 `body` 폭이
  455px까지 벌어져 가로 스크롤이 생기는 게 실제로 재현됨. 원인은 CSS Grid의 기본 동작
  (`min-width: auto`) — 그리드 아이템(`.scr-field`)이나 그 안의 `<select>`/`<input>`에
  `width:100%`/`min-width:0`이 없으면, 내용의 최소 크기 아래로 줄어들지 않아 트랙 자체가
  화면 밖으로 밀려난다. `.scr-field`·`.scr-field input,select`에 `min-width:0`(+
  `width:100%; box-sizing:border-box`)을 추가해 근본 수정. 같은 원리로 종목상세의
  "🪜 단계별 매수 전략"(`.buy-plan-stages`, 오늘 P0 작업에서 만든 기능)도 "1,840,800원"
  처럼 끊어질 지점이 없는 긴 숫자 문자열 때문에 같은 방식으로 375px에서 429px까지
  넘치고 있던 걸 함께 발견해 수정(`.buy-stage`에 `min-width:0`, 가격에
  `overflow-wrap:anywhere` 안전장치).
- **모바일 폼 레이아웃 재설계**: 2열이던 모바일 그리드를 1열로 변경(라벨이 "목표가
  상승여력(%) ≥"처럼 길어 2열은 여전히 빡빡함). 검색/초기화 버튼은 세로로 쌓아 전체
  너비로. 결과 행에 붙인 PER·ROE 부가정보(`<span class="scr-extra">`)는 모바일에서
  숨겨 랭킹표와 같은 줄 수를 유지(안 그러면 한 줄에 다 안 들어가 행 높이가 들쭉날쭉
  길어짐).
- **홈 화면: 관심종목을 맨 위로, quick-picks 제거**: `.hero` 바로 아래 있던 하드코딩
  추천종목 6개(삼성전자·SK하이닉스·삼성바이오로직스·LG에너지솔루션·NAVER·크래프톤)
  버튼을 완전히 삭제하고, 페이지 맨 아래쪽(이상징후 포착 위)에 있던 관심종목(`#fav-board`)
  섹션을 히어로 바로 다음, 다른 모든 홈 섹션(내 포트폴리오·오늘의 AI 투자기회·테마·
  백테스트 등)보다 앞으로 이동. 관심종목이 하나도 없으면 기존 로직 그대로 섹션 자체가
  숨겨진다(로그인 불필요, localStorage 기반 — 새 사용자는 그냥 안 보임, 동작은 안 바꿈).
  죽은 JS 바인딩(`.quick-picks button` 클릭 핸들러)과 미사용 CSS도 함께 정리.
- 검증: 모바일 375px에서 스크리너 폼 그리드 열 폭(`getComputedStyle().gridTemplateColumns`)
  이 컨테이너 폭을 넘지 않는지, `document.body.scrollWidth`가 뷰포트와 같은지 직접 측정해
  확인(수정 전 455px→수정 후 375px). 종목상세 페이지도 동일하게 확인(429px→379px, 남은
  4px는 무관한 배경 장식 요소 `.bg-orbs`로 실제 콘텐츠와 무관함을 확인). 홈 화면 자식
  요소 순서(`landing.children`)를 직접 읽어 관심종목이 히어로 다음(두 번째)에 오는지,
  관심종목에 실제로 종목을 담아 렌더링·클릭까지 확인. 데스크톱에서 스크리너 그리드가
  기존처럼 여러 열로 정상 동작하는지도 함께 확인(모바일 전용 변경이 데스크톱에 영향
  없음).

### 3.15 고급 차트 4대 기법 — Anchored VWAP·Liquidity Sweep·Volume Profile·Order Flow (2026-08-19, v60)
사용자: "ANCHORED VWAP, LIQIDITY SWEEP, VOLUME PROFILE, ORDER FLOW 4개 사이트
벤치마킹해서 고급차트 고도화해줘... 저 사이트에서 사용하고 있는 기법이 있다면 그대로
표현해도 좋아." `app/chart_pro.py`(기존 스테이지·상대강도·추세템플릿·VCP·OBV·피보나치
10개 기법의 "고급 차트 분석" 레이어)에 4개 기법을 추가하고, 점수뿐 아니라 캔들차트에
직접 오버레이로 그려 실전 트레이딩 플랫폼처럼 보이게 만들었다.

- **⚠️ 데이터 한계부터 명시**: 이 프로젝트는 네이버의 **일봉 OHLCV**만 쓸 수 있고
  틱·호가(Level 2) 데이터가 없다. Anchored VWAP·Liquidity Sweep·Volume Profile은
  일봉만으로도 원래 기법 그대로 정확히 계산 가능하지만, **Order Flow(오더플로우)는
  원래 매수/매도 체결 데이터가 있어야 하는 기법이라 진짜 오더플로우는 계산이 불가능하다**.
  대신 CLV(Close Location Value — 그 날 저가~고가 범위 안에서 종가의 위치)에 거래량을
  곱한 근사치(Chaikin Money Flow와 동일 원리)를 만들고, 라벨·툴팁·데이터 어디에도
  "근사치"임을 숨기지 않았다(이 프로젝트의 일관된 원칙).
- **Anchored VWAP** (`anchored_vwap()`) — 브라이언 섀넌 방식. 실적발표일처럼 이 프로젝트가
  명확히 태깅하지 못하는 이벤트 대신, 객관적으로 계산 가능한 3개 앵커(52주 신고가·
  52주 신저가·연초 YTD)부터 각각의 거래량가중평균가 시계열을 계산. 현재가가 각 앵커
  대비 위/아래인지로 "그 시점 이후 진입자가 평균적으로 수익/손실 중인지"를 판단(지지/
  저항 해석). 차트에 3개 색선(핑크=52주 신고가, 시안=52주 신저가, 옐로=연초)으로 직접
  표시.
- **Liquidity Sweep** (`liquidity_sweeps()`, ICT/Smart Money Concepts) — 프랙탈 피벗으로
  스윙 고점/저점을 찾고, 그 레벨을 살짝 뚫었다가 반대 방향으로 마감하는 캔들을 탐지
  (스윙고점 스윕 후 하락마감=약세, 스윙저점 스윕 후 상승마감=강세 — "유동성 풀"에 몰린
  손절 주문을 훑고 반전한다는 스마트머니 컨셉트 그대로). 캔들차트에 🧲 마커로 표시(기존
  골든/데드크로스 마커와 같은 배열에 합쳐서 `setMarkers()` 한 번만 호출 — lightweight-
  charts v4는 시리즈당 마커셋이 하나뿐이라 따로 부르면 덮어써진다).
- **Volume Profile** (`volume_profile()`) — 가격대별 거래량 분포(POC·Value Area 70%).
  ⚠️ 진짜 볼륨 프로파일은 틱 데이터로 만들지만, 일봉만 있어 하루 거래량을 그 날의
  고가~저가 범위에 균등 분배하는 근사법을 썼다(분봉 이하 데이터가 없을 때 실전에서도
  흔히 쓰는 방식). **lightweight-charts엔 이 기능이 없어** 별도 `<canvas id="vp-overlay">`
  를 `.chart-wrap`에 얹고, `candleSeries.priceToCoordinate(price)`로 각 가격 구간을
  실제 화면 y좌표로 변환해 오른쪽 정렬 가로 막대로 직접 그렸다(POC는 노란선, VAH/VAL은
  점선). 확대/축소·기간 버튼 클릭 시 가격축이 바뀌므로 `subscribeVisibleLogicalRangeChange`
  에 재계산을 걸어 막대가 안 어긋나게 함.
- **Order Flow 근사** (`order_flow_proxy()`) — 위 데이터 한계 설명 참고. 누적 델타 추세와
  가격 다이버전스(OBV와 같은 원리지만 CLV 기반이라 더 정밀)를 "고급 차트 분석" 카드에
  인라인 SVG 스파크라인(`sparklineSvg()`)과 함께 표시, 라벨에 ⓘ 툴팁으로 근사치임을
  명시.
- **오버레이 켜기/끄기**: 3개 오버레이(AVWAP·유동성스윕·볼륨프로파일)를 한 화면에 다
  띄우면 캔들·이평선·목표가선까지 겹쳐 과할 수 있어, `.chart-overlay-toggles` 칩
  버튼으로 개별 on/off 가능하게 함(기본값 전부 ON — "그대로 표현해도 좋다"는 요청에
  맞춰 기본은 보여주되, 원치 않으면 끌 수 있게). 토글은 상태만 바꾸고 `drawChart()`를
  다시 불러 반영(가장 단순하고 안전한 방식).
- **주봉/월봉 미지원**: `chart_pro`는 일봉 기준으로만 계산되므로(앵커 날짜·봉 인덱스가
  주/월봉과 안 맞음) 이 4개 오버레이는 일봉(`tf==="day"`)일 때만 그린다.
- **종합 점수 재조정**: 기존 6개 항목 가중치(스테이지 0.30·추세템플릿 0.25·상대강도
  0.20·VCP 0.10·OBV 0.10·박스 0.05)에 새 4개를 얹으며 전체 재조정 — 스테이지 0.22·
  추세템플릿 0.19·상대강도 0.15·VCP 0.08·OBV 0.07·박스 0.04·AVWAP 0.08·유동성스윕
  0.06·볼륨프로파일 0.05·오더플로우근사 0.06(합 1.00). 새 기법들이 과반을 차지하지
  않도록 기존 검증된 기법 비중을 더 크게 유지(CLAUDE.md 9장 원칙 — 점수화 편향 재검증).
- 검증: 로컬에서 SK하이닉스·삼성전자·크래프톤(짧은 이력)·AAPL 4종목으로 `chart_pro.analyze()`
  직접 호출해 전부 에러 없이 계산됨을 확인, 신호 텍스트가 실제 데이터와 부합하는지 확인
  (예: SK하이닉스가 6월 고점 2,987,000원에서 급락한 뒤라 AVWAP 52주 신고가 기준으로는
  아래, 신저가·연초 기준으로는 위인 "엇갈림" 상태가 정확히 잡힘). 브라우저에서 볼륨
  프로파일 캔버스에 실제 픽셀이 그려지는지(`getImageData`로 non-transparent 픽셀 수
  확인), 토글 버튼으로 캔버스가 꺼지고 다시 켜지는지, 모바일 375px에서 가로 스크롤 없이
  반응형으로 축소되는지 확인. 콘솔 에러 없음(기존 무관 노이즈 제외).
  ⚠️ **검증 한계**: 이 자동화 브라우저 환경은 기간 버튼을 눌러도 실제 가격축 재계산이
  일어나는지(볼륨 프로파일 막대의 y좌표가 실제로 바뀌는지)까지는 확인 못 했다 —
  `setVisibleLogicalRange()` 자체가 이 환경에서 반영이 안 되는 기존에 확인된 한계
  (3.11장)와 같은 원인으로 추정. 메커니즘(`priceToCoordinate` 기반 좌표 계산, 이벤트
  구독)은 공식 API 그대로라 신뢰도는 높음 — 실제 브라우저에서 기간 버튼을 눌러 볼륨
  프로파일 막대가 따라 움직이는지 최종 확인 권장.
- **응답 크기**: `/api/analyze` 응답이 130KB→155KB로 커짐(AVWAP 시계열 3개 + 오더플로우
  누적 델타 등 추가). 리포트 P2가 지적한 "캔들 데이터를 별도 엔드포인트로 분리" 작업을
  하면 자연히 같이 줄어들 것 — 이번 작업 범위에는 포함하지 않음.

### 3.16 2차 진단리포트 P0 8건 — 유료화 직전 필수 결함 (2026-08-19, v61)
사용자가 `StockLens_2차진단리포트_1.docx`를 전달. 로그인 상태 실사용 기준 재감사로,
1차 지적 대부분(추정실적 이상치 검증·단계별매수 상태표시·차트기간필터·스크리너 등)은
반영 확인됐으나, "기능이 켜져 있는 것처럼 보이는데 실제로는 동작하지 않는다"·"같은
화면이 서로 다른 답을 준다" 유형의 새 문제를 다수 지적. P0(유료화 전 필수, 8건)만
이번에 전부 수정. P1(라우팅 확장·거래이력·벤치마크·법규 등, 결제수단·사업자정보·법률
자문이 필요해 직접 처리 불가한 항목 포함)·P2는 미착수.

- **① 알림 권한 요청 무응답 (가장 심각한 지적)** — `Notification.requestPermission()`이
  응답 없이 멈추면 "권한을 요청하는 중..."에서 영원히 정지, 이후 `/api/watch` POST에
  도달하지 못해 관심종목 저장 자체가 안 됐다(리포트가 "두 번 토글해도 items 빈 배열"로
  실측). 관심종목 저장을 푸시 구독과 완전히 분리해 **항상 먼저** 확정하고, 푸시는
  별개 시도로 바꿈. `Notification.requestPermission()`·`serviceWorker.register`·
  `ready`·`pushManager.subscribe` 각각에 `withTimeout()`(10~15초)을 걸어 절대 무응답
  상태로 남지 않게 함. `Notification.permission === "denied"`면 요청 자체를 생략하고
  바로 안내. (`static/app.js` `ensurePushSubscribed`, `watch-btn` 핸들러)
- **② 포트폴리오 점수가 리스크를 반영하지 않음** — 예전엔 `score`가 보유종목 점수의
  가중평균뿐이라, 경고 3건(반도체 100%·삼성전자 100%·변동성 73%)을 띄우면서도
  "B등급·양호"였다. 집중도(허핀달-허쉬만 지수, N종목 균등분산=1/N 기준선)·변동성·
  최대낙폭 감점을 반영한 `_risk_penalty()`를 신설해 `종합점수 = 종목 품질 점수 −
  리스크 감점`으로 재정의. UI에도 두 숫자를 분리 표시(`quality_score`/`risk_penalty`/
  `risk_penalty_detail`). 로컬 검증: SK하이닉스 100%·변동성 89%·낙폭 -55% 포트폴리오가
  품질 84.7점(A급)에서 감점 62.4점을 받아 최종 22.3점(F등급·위험)으로 정확히 강등됨.
  (`app/portfolio.py` `_risk_penalty`, `compute`)
- **③ 홈 요약과 포트폴리오 페이지가 정반대 메시지** — 홈 위젯이 `today_actions`만
  읽고 `warnings`를 무시해서, 종목이 1개뿐이라 `today_actions`가 비면("비중 과다"는
  비교 대상 종목이 있어야 함) 홈은 "적정 수준 유지"라는데 포트폴리오 페이지는 경고
  3건 + 손실 -22.7%를 보여줬다. `warnings`도 함께 반영하도록 수정. 로컬 검증: 단일
  SK하이닉스 100% 포트폴리오에서 홈 위젯이 이제 "주의할 점 3건" + 경고 목록 3개를
  정확히 보여줌(예전엔 "🟢 적정 수준 유지"). (`static/app.js` `mypfActionsHtml`)
- **④ 목표 비중이 한 화면에서 두 가지로 제시됨** — "오늘의 Action"은 균등분산
  기준(100%/종목수)을, "AI 리밸런싱"은 AI판단 가중치 기준을 따로 계산해서 같은
  페이지에 다른 숫자가 동시에 떴다(리포트 예시: 38%p 축소 vs 44%p 축소). `_today_actions()`
  가 이미 계산된 `target_weight`(AI 리밸런싱과 동일 소스)를 그대로 쓰도록 통일. 로컬
  검증: 삼성전자+엔비디아 조합에서 "오늘의 Action"과 "AI 리밸런싱" 둘 다 정확히 같은
  숫자(50%)로 일치. (`app/portfolio.py` `_today_actions`)
- **⑤ 삭제 확인 없음 + 수정 기능 없음** — 보유종목 행에 [삭제]뿐이라 잘못 입력하면
  지우고 다시 넣는 수밖에 없었고, `confirm()` 호출이 코드 전체에 0건이라 즉시·되돌릴
  수 없이 삭제됐다. `confirm()` 추가, [수정] 버튼으로 해당 행을 인라인 입력칸으로
  바꿔 `PUT /api/portfolio/{code}`(새 `portfolio.set_holding()` — 기존 `upsert()`와
  달리 더하지 않고 덮어씀)로 저장하는 기능 추가. 브라우저에서 confirm() 취소 시
  삭제 안 됨, 저장 시 수량/평균단가가 실제로 갱신됨을 확인. (`app/portfolio.py`
  `set_holding`, `main.py` `PUT /api/portfolio/{code}`, `static/app.js` 인라인 수정)
- **⑥ ETF 추가 무음 실패** — `portfolio.compute()`의 `_fetch()`가 `analyze_fn()` 예외를
  `except Exception: continue`로 그냥 삼켜서, 서버 저장은 됐는데 분석이 실패하는
  종목(예: TIGER 삼성전자단일종목레버리지)이 목록에서 이유 없이 사라졌다. 예외를
  `excluded`에 사유와 함께 담도록 수정(성공/사유 둘 다 무조건 표시). 프론트도 추가
  직후 `excluded`를 확인해 사용자에게 그대로 알림. 실측: 이 ETF는 네이버 API가
  `basic` 엔드포인트에서 409를 반환해 원천적으로 분석이 안 되는 종목임을 확인 —
  "무음 실패"였던 게 이제 "⚠️ 저장은 됐지만 목록에는 표시되지 않았습니다 (분석 실패:
  ...)"로 명확히 뜸. (`app/portfolio.py` `compute`, `static/app.js` `pf-add-btn`)
- **⑦ 손절가가 3차 매수가보다 위** — 손절가(기술적 지지선×0.96)와 3차 매수가(밸류에이션
  적정가×0.80)가 서로 다른 모델이라 우연히 손절가가 3차 매수가보다 높아지는 경우가
  있었다(리포트 실측: 3차 1,316,277원 vs 손절 1,318,080원). `api_analyze()`에서 둘 다
  계산된 뒤 손절가가 3차 매수가 이상이면 `3차 매수가 × 0.97`로 강제 하향. 로컬+라이브
  SK하이닉스로 확인(손절 1,192,502원 < 3차 매수 1,229,383원). (`main.py`)
- **⑧ 목표주가에 이상치 검증이 빠져 있음** — 재무 추정치엔 검증 레이어가 있는데
  목표주가(컨센서스)는 그대로 노출돼 SK하이닉스 +120% 같은 값이 "왜 사야 하나" 최상단에
  떴다. `analysis.consensus_info()`에 `TARGET_UPSIDE_OUTLIER=60%` 기준 플래그를
  추가하고, 괴리가 클수록(`upside_weight`, 최저 0.3배) 확신도 계산(`final_verdict`)과
  적정가 산정(`valuation.fair_buy_price`)에서의 반영 비중을 선형으로 낮춤(완전 제외는
  아님 — 목표가 자체는 유효 정보이므로). 화면에는 목표주가 기준일(`consensus.date`,
  전에는 API에 있었지만 화면에 전혀 안 쓰이고 있었음)과 괴리 과대 배지(⚠️)를 표시.
  라이브 확인: SK하이닉스 목표주가 3,317,917원(2026-08-18 기준), 상승여력 +119.9%,
  `upside_flagged=true`. (`app/analysis.py` `consensus_info`/`final_verdict`,
  `app/valuation.py` `fair_buy_price`, `static/app.js` hl-grid·targets 탭)

**검증 방식**: 로컬(uvicorn :8899)에서 signup/login 세션으로 SK하이닉스+엔비디아
포트폴리오를 실제로 만들어 ②③④⑤⑥ 전부 API+브라우저(javascript_tool DOM eval)로
end-to-end 확인 후 배포. 라이브는 ⑦⑧을 `/api/analyze/000660` 응답으로, 캐시버전(v61)과
콘솔 에러 없음을 브라우저로 재확인. ①은 실제 브라우저 알림 권한 팝업 자체는 이 자동화
환경에서 항상 `Notification.permission === "denied"`로 나와(팝업이 뜰 수 없는 헤드리스
환경) 정상 승인 경로까지는 못 봤지만, **denied 분기**(무응답 없이 즉시 명확한 안내 +
관심종목은 정상 저장됨)는 실측 확인됨 — 실제 사용자 브라우저에서의 승인/거부 흐름은
별도 수동 확인 권장.

**보류(P1/P2, 이번 범위 밖)**: 라우팅 전 페이지 확장·서버사이드 OG태그·robots.txt·
sitemap.xml, 매수일자/거래이력 저장, 현금(예수금) 항목, 벤치마크 대비 수익률, 한·미
거래일 정렬 후 상관관계 재계산, 소급 백테스트 공개, 재무표 단위 표기, 리밸런싱 업종
제약, 국가별/환노출 비중, 소수점 주식, 토스트 피드백 체계. **약관·개인정보처리방침·
사업자정보·유사투자자문업 신고·정식 도메인**은 결제수단·실제 사업자 정보·법률 자문이
필요해 AI가 대신 처리할 수 없는 항목 — 사용자 확인 필요.

### 3.17 오늘의 AI 투자기회 카드 개편 (2026-08-19, v62)
사용자 지적: "종합점수 ≠ 투자기회" 범례 칩과 "지금 사기 좋은 종목" 태그라인만 있고
왜 이 종목이 오늘의 PICK인지 근거가 없다, 즐겨찾기가 안 된다, 상승여력이 애널리스트
목표가 평균뿐이라 근거가 약하다. 범례·태그라인을 지우고 `opportunityReasons()`로 이미
계산된 근거(상승여력·RSI·PER저평가·수급·실적전망)를 문장으로 풀어 표시, 카드 우측
상단에 즐겨찾기(★, localStorage 기반) 버튼 추가. "적정주가는 어떻게 나왔나" 섹션
신설 — 과거 대비 PER/FPER 밴드·PEG·동종업계 PER·EV/EBITDA·PBR(참고)을 노출. 상승여력은
밸류에이션 적정가(fair_buy.optimistic.upside) 기준을 주 지표로, 애널리스트 목표가
기준은 참고치로 병기(괴리 과대 시 ⚠️ 배지). 라이브 SK하이닉스로 확인: 밸류에이션
기준 +1.8% vs 애널리스트 기준 +120.3%⚠️ — 두 수치가 크게 갈리는 사례가 실제로 잡힘.

### 3.18 관심종목(즐겨찾기) 목록 재구성 (2026-08-19, v63)
사용자 지적: "관심종목 너무 대충 구현한 것 같다 — 불빛 다 치우고 정말 필요한 정보
위주로." 이름·현재가·AI판단 3개뿐이던 카드를 실시간 랭킹(.rank-row)·오늘의 PICK
(.today-row)과 같은 밀도의 표로 재구성 — 판단·종목명+코드·종합점수·현재가+등락률·
상승여력, 모바일은 종합점수·상승여력 열 숨김. hover 시 그라디언트 글로우+이동
효과("불빛")는 제거하고 단순 배경 전환만 남김.

### 3.19 AI 최종판단 8단계로 세분화 (2026-08-19, v64)
사용자 지적: "매수, 보유, 분할매수 이렇게 하지 말고 더 직관적이고 세분화해서." 5단계
(매수/분할매수/보유/분할매도/매도)를 8단계로 확장 — 적극 매수(88+)·매수(76+)·매수
관심(64+)·분할 매수(52+)·보유·관망(40+)·비중 축소 검토(28+)·분할 매도(16+)·매도(0+).
`analysis.py` VERDICT_TIERS 확장 + 확신도 계산의 매수/매도 방향 판정을 하드코딩 대신
`_BULLISH_TIERS`/`_BEARISH_TIERS` 집합으로 일반화. `portfolio.py` `_TIER_WEIGHT`(AI
리밸런싱 배점)와 `app.js` `verdictColor()`도 8단계에 맞춰 보간. 실측: 62.4점 "분할
매수" vs 67.6점 "매수 관심" — 구 5단계에서는 둘 다 같은 구간이었음.

### 3.20 3차 진단리포트 P0 7건 — 점수 산식 설계 결함 (2026-08-19, v65)
사용자가 `StockLens_3차진단리포트.docx` 전달. 이번 리포트의 결론은 "계산 자체는
깨끗하다"(371종목 전수 회귀 검증, 평균 오차 0.024점) — 문제는 계산이 아니라 **설계**
쪽이라는 점이 1·2차와 달랐다. P0 7개 항목 전부 수정.

- **① 성장률 분모(전기 실적)가 적자면 증가율이 무의미** — `_growth(cur, prev)`가
  `abs(prev)`로 나눠서 전기 적자(-100→-10)도 "+90% 성장"처럼 계산됐다. 엘앤에프(ROE
  -76.9%·영업적자)가 성장성 89.4점을 받는 실제 사례. `prev<=0`이면 None 반환 +
  `_growth_status_label()`로 "흑자전환/적자축소/적자확대" 라벨 대체. 현재 영업적자
  상태면 성장성 점수에 영업이익률 연동 상한(`_clamp(50+opm,20,50)`)도 추가. 실측:
  엘앤에프 89.4→42.7점(총점 36.6·D), 성일하이텍 89.3→22.0점(31.1·F), 케이엠더블유
  84.9→25.6점(32.2·F). 심텍은 opm이 +0.84%(막 흑자전환)라 상한이 걸리지 않아
  90.3→80.7점으로 부분 개선에 그침 — op_cur가 실제로 양수인 경계 케이스라 의도된 동작.
- **② 판단이 가격을 안 봄** — 종합점수만으로 tier를 정해서 목표주가보다 32% 비싼
  종목이 "매수"였다(뉴몬트 -31.9%, 일라이릴리 -5.7%). `final_verdict()`에
  damped_upside(2차 리포트 때 만든 upside_weight 적용값) 기준 1~2단계 보정 추가.
  **상향 보정은 목표가 자체가 이상치 플래그된 경우 적용 안 함** — SK하이닉스처럼
  목표가 신뢰도가 이미 낮다고 판정된 상태에서 그 수치로 판단을 더 낙관적으로 올리면
  이상치 검증의 취지와 어긋나기 때문(실측: SK하이닉스는 damped_upside=60 근처라
  보정 없이 "strong_buy"로 잘못 올라갔던 걸 이 가드로 "buy"로 되돌림). 실측: 뉴몬트
  "매수"→"분할 매수"(2단계↓), 일라이릴리 "매수"→"매수 관심"(1단계↓).
- **③ 등급 경계가 반올림 전 원값 기준** — 녹십자 50.0점 D, BNK금융지주 50.0점 C처럼
  "표시는 같은데 등급이 다른" 문제. `total_evaluation()`·`valuation.analyze()`·
  `technical_analysis()` 3곳 모두 반올림(표시값) 후에 등급을 판정하도록 수정
  (portfolio.py는 이미 올바르게 구현돼 있었음).
- **④ "제외했다" 배너 아래 성장성이 여전히 100점** — 컨센서스 이상치 배너와 확정
  실적 재계산 결과가 같은 화면에 있는데 어느 것도 설명이 없었다. 성장성 막대 옆에
  "확정 실적 기준: 매출 +46.8%, 영업이익 +101.2%"를 병기.
- **⑤ PEG 문구가 실제 동작과 다름** — "PEG···점수에서 제외"라고 말하지만 실제로는
  PEG가 확정 실적 기준으로 자동 재계산돼 점수에 그대로 반영되고 있었다(문구 오류,
  실제 제외 아님). 문구를 사실에 맞게 수정하고, 상한 절단된 성장률에 원본값 병기
  (`"50.0% (상한 적용, 실제 101%)"`).
- **⑥ 포트폴리오 리스크 감점 과잉 교정** — 품질 68.8점 단일 보유가 F등급 12.3점까지
  떨어짐(감점이 품질의 82%). 집중도 감점을 볼록곡선(지수 1.6, 기준선 0.25)으로 바꿔
  3~5종목 정상 범위(HHI 0.2~0.35)는 완만하고 극단 집중(HHI→1.0)만 가파르게. 변동성·
  최대낙폭은 상관관계가 높아 사실상 같은 위험의 이중 계산이었던 걸 "큰 쪽 전부 + 작은
  쪽 30%"로 정리. 보유종목 1~2개일 때 AI 리밸런싱이 "적정"이라고 오도하던 문구도
  "종목이 적어 비교가 무의미하다"로 수정. 실측: 동일 SK하이닉스 100% 케이스가
  감점 56.5→37.5점, 12.3점·F → 46.4점·D로 완화(여전히 리스크는 명확히 반영됨).
- **⑦ 알림 권한 요청 여전히 무응답** — 2차 리포트 대응 때 "관심종목 저장을 먼저"로
  순서를 바꾼 게 회귀의 원인이었다. 그 `await` 때문에 클릭의 user-activation이
  만료돼 `Notification.requestPermission()`이 프롬프트를 아예 안 띄우고(permission이
  계속 default) 응답도 없었다. 권한 요청을 클릭 직후 다른 await 없이 가장 먼저
  호출하도록 순서를 되돌리고, 관심종목 저장은 그 다음(성공 여부 무관하게 항상 수행)
  으로 재배치 — "먼저 저장 확정"과 "권한 요청은 활성화 유지 중 최우선 호출" 두 요구를
  순서 재배치로 동시에 만족시켰다.
- **부가**: ETF 등 분석 실패 시 오류 문구에 내부 API 주소(`api.stock.naver.com/...`)가
  그대로 노출되던 것도 정규식으로 제거.

**보류(P1/P2)**: 카테고리 점수 백분위 변환(명목=실효 가중치 일치), 가중치 화면 공개 +
산식 문서 페이지, 랭킹/상세 점수 기준시각 통일, AI 리밸런싱 종목·업종 상한, 소급
백테스트, 라우팅 확장, 약관·개인정보처리방침·사업자정보·정식 도메인(3회 연속 지적,
AI가 대신 처리 불가), 재무표 단위 표기, 토스트 피드백 체계.

### 3.21 트레이딩엔진 설계서 반영 (2026-08-20, v66→v73)
차트프로(`chart_pro.py`)에 구현만 돼 있고 점수화엔 안 쓰이던 기법들을 실제로 연결.
- **볼륨 프로파일 재구현** + 수급 데이터 조회기간 10일→60일 확장
- **AVWAP 8앵커 확장·재점수화** + 유동성 스윕(liquidity sweep) 강도 판정 고도화
- **수급 오더플로우 모듈 신설** — 기존 근사 로직 폐기하고 교체
- **컨플루언스 엔진 신설** — AVWAP·스윕·볼륨프로파일·오더플로우 4개 기법의 결과가
  겹치는 가격대를 지지/저항으로 자동 도출
- 차트 UI 오버레이: AVWAP 라벨·스윕 강도·수급 패널·컨플루언스·Y축 서식
- 차트프로 종합점수 가중치를 화면에 병기(어떤 기법이 얼마나 반영됐는지 투명하게 노출)
- 포트폴리오 보유종목 섹션을 총액 카드 바로 다음으로 이동, 모바일 좌우 흔들림 버그 수정

### 3.22 기술점수 고도화 + 실시간성 개선 (2026-08-20, v73→v75)
- **기술점수에 컨플루언스 반영 + 신호 일치도 보정, 스윕/볼륨프로파일 점수 고도화**
  (`app/chart_pro.py`) — 사용자 지적: "트레이딩 기법을 적용시켜놨는데 제대로 활용은
  못하는 것 같다". `confluence_proximity()` 신설(가격이 강한 컨플루언스 지지/저항에
  가까울수록 가점), `analyze()`에 부문 간 합의(conviction) 보너스 추가(4개 이상 부문
  중 다수가 같은 방향이면 최대 +6점), `volume_profile()`을 20/60/120일 다중 lookback
  중 신뢰도 "high"인 것을 우선 채택하도록 재작성, `liquidity_sweeps()`에 표본 10건
  미만이면 신뢰도 배수를 적용 안 하는 가드(`MIN_SAMPLE`) 추가.
- **실시간 시세 갱신 지연 단축** — 사용자 지적: "실시간시세가 아닌 것 같다, 오차없이
  바로바로". `app/naver.py` 시세 캐시 TTL 5s→2s, `app.js` 폴링 주기 4s→2s.
- **오늘의 PICK 4개 상자박스 크기 통일** — 사용자 지적(스크린샷): "목표주가 상승여력
  상자박스가 너무 크다, 4개 다 똑같은 크기로". `.today-pick-stats div`에
  `flex-direction:column; align-items:center; justify-content:center` 적용.

### 3.23 4차 진단리포트 P0+P1 반영 (2026-08-20, v75→v76)
사용자가 `StockLens_4차진단리포트.docx` 전달. `app/chart_pro.py`·`app/analysis.py`
중심으로 데이터 정확성·신뢰도 항목 반영:
- `volume_profile()`을 lookback 후보군(20/60/120일) 중 신뢰도 기준으로 고르는
  래퍼로 재구성, 추세 구간이라 가치영역 판정이 무의미할 땐 "참고용" 안내로 대체
- `liquidity_sweeps()` 감지 조건 재조정(거래량 임계 1.5x→1.2x, 꼬리비율 50%→40%,
  되돌림 확인을 당일 종가 대신 이후 3봉 이내로 완화) + 표본 부족 시 신뢰도 배수 가드
- `darvas_box()`에 `reliable`(박스폭 60 이하) 플래그 추가 — 신뢰 불가 박스는 중립 신호로 대체
- `smart_money_flow()` 다이버전스 윈도우를 고정 21일 대신 데이터 길이에 맞춰 동적
  계산, 매도 연속(`sell_streak_foreign`) 추가로 매수/매도 스트릭을 대칭 반영
- `confluence()` 클러스터링을 "패밀리(기법군)별 최고값 합산 + 기법 다양성 보너스"로
  재작성(같은 기법 반복 카운트 방지), 지지/저항 각 1건을 `confluence_support`/
  `confluence_resistance`로 노출 — `main.py`에서 기존 `tech.support`/`resistance`는
  안 건드리고 `tech.support_confluence`/`resistance_confluence`로 **추가만** 함(매수존·
  손절가 계산 영향 없음)
- 성장성 점수에 영업이익률 연동 상한 보강(`analysis.py`), PEG 체크리스트 문구를
  실제 계산 로직과 일치하도록 수정(`valuation.py`)
- **보류**: entry_plan/지지·저항선 전면 교체(기존 매수존·손절가와 불일치 위험),
  공매도·대차잔고 데이터, 수급 120일 확장(네이버 API가 60일 초과 시 400 반환 — 외부
  제약), OHLC 툴팁(의도된 동작으로 판단)

### 3.24 UI/UX 진단보고서 반영 1부 — 디자인시스템·접근성 (2026-08-20, v76→v82)
사용자가 `StockLens_UIUX_진단보고서.docx` 전달. 라이브 DOM을 직접 `getComputedStyle`로
측정해 리포트의 원인 진단 2건이 실제로는 틀렸음을 확인하고 정정 후 진행(라이트모드가
CSS 변수를 안 덮어쓴다는 주장 → 실측상 정상 오버라이드 중이었고, 진짜 원인은 다른 곳).
- **디자인 토큰**: 타이포 스케일(`--fs-2xs~3xl`), 스페이싱 스케일(`--sp-1~10`) 도입
- **라이트모드 WCAG AA 대비 수정**: 진짜 원인은 (1) `--up`/`--down`/`--green`/`--amber`/
  `--accent` 등 시맨틱 색이 다크 배경 전용으로만 튜닝돼 라이트 변형이 아예 없었던 것,
  (2) `scoreColor()`/`verdictColor()`가 JS로 인라인 `style="color:#hex"`를 박아 넣어
  CSS 변수 오버라이드가 아예 닿지 않는 경로였던 것. 커스텀 대비 감사 스크립트를 만들어
  라이브 DOM을 두 테마 모두 실측(카드처럼 반투명 배경 위에서 재측정 — 첫 판정은 통과했지만
  실제 카드 위에선 4.37:1로 미달하는 값들이 있었음), 목표 대비 5.3:1 이상으로 재조정
  (`--up:#c2183a`, `--green:#076b41`, `--accent2:#0b5f76` 등). `scoreColor()`/
  `verdictColor()`를 `var(--sc-*)`/`var(--vd-*)` 문자열을 반환하도록 재작성.
- **sticky 상세 탭바 신설** — 스크롤 시 탭바가 화면에 고정. 구현 중
  `body{overflow-x:hidden}`이 `position:sticky`를 통째로 깨는 걸 발견(overflow를 주면
  body가 스크롤 컨테이너가 돼 자식의 sticky가 무효화됨) — 규칙을 `body`에서 `html`로
  옮겨 해결(가로 스크롤 방지 효과는 유지하면서 sticky 복구).
- **접근성**: `:focus-visible` 링 복원(3곳에서 `outline:none`만 걸고 대체 표시가
  없었음), 터치 타깃 44px 가이드라인 — 다만 밀집된 관심종목 행 아이콘(24px+5px간격)에
  `::after` 히트영역 확장을 그대로 쓰면 44px 영역끼리 15px씩 겹쳐 마지막 버튼(삭제)이
  DOM 순서상 위로 올라와 오히려 오조작이 늘어남을 계산으로 미리 발견 → 밀집 구간은
  실제 크기 확대(24→34px), 여백이 있는 단독 버튼만 `::after` 확장 사용.
  `prefers-reduced-motion`, `.sr-only`, alert() 9곳 → `showToast()`(`role`/`aria-live`)로 교체.

### 3.25 UI/UX 진단보고서 반영 2부 — 정보구조·용어·아이콘 (2026-08-20, v82→v86)
- **홈 3영역 재편**(5.9화면→4.3화면): 📌내 투자 / 🔎오늘의 발견 / 🧭둘러보기로 재배치,
  섹션 접기/펼치기 상태를 `localStorage`에 저장, 사용자 지시("관심종목을 홈 맨 위로")
  반영해 내 투자 영역 최상단에 배치
- **전용 페이지 신설**: `/watchlist`·`/screener`·`/portfolio`가 그동안 404였던 걸
  중앙 라우터(`ROUTES`/`matchRoute()`/`navigate()`, `data-route` 속성 위임,
  `history.pushState`) 신설로 해결. 직접 URL 진입 시 `loadMe()` 완료 전에 인증가드가
  먼저 실행돼 로그인된 사용자도 로그인 모달이 뜨는 레이스 컨디션을 `_meReady` Promise로
  수정, 가드가 뜬 뒤 URL이 `/watchlist`에 멈춰 새로고침마다 재발하던 것도
  `history.replaceState(null,"","/")`로 정리
- **용어 통일(8종)**: 적정매수가→매수 적정가, 적정가/적정주가→적정가치, 매수 관심
  구간→지지 밴드, 1·2·3차 매수→분할 진입 N차, 목표주가→컨센서스 목표가, 기술적
  목표가→기술적 저항가. **배포 직전에 잡은 크래시**: `valuation.py`에서
  `parts["목표주가"]`만 바꾸고 `weights` 딕셔너리의 같은 키를 안 바꿔서 밸류에이션
  계산 전체가 KeyError로 죽을 뻔했음 — 국내/미국 3종목 재계산으로 검증 후 배포.
- **이모지 → SVG 아이콘**: 28개 심볼 인라인 스프라이트 신설(`stroke=currentColor`로
  테마 자동 추종), 네비·섹션제목·아이콘버튼 등 "화면 골격" 35곳만 교체 — 🟢🟡🔴 판단
  티어·🇰🇷🇺🇸 국가·✅⚠️ 근거 불릿처럼 **색으로 상태를 전달하는 나머지는 의도적으로
  이모지 유지**(선형 아이콘으로 바꾸면 정보 손실).

### 3.26 UI 세부 결함 4건 (2026-08-20~21, v86→v88 · js v94→v97)
- **상단 네비 아이콘 불일치** — 홈·종목분석 두 탭만 아이콘이 없어 나머지 3개와 리듬이
  안 맞던 것. `i-home` 아이콘 신설, 종목분석엔 `i-chart` 재사용, 네비 버튼에 flex+gap.
- **관심종목 행 액션버튼-상승여력 겹침** — 터치타깃 확대(24→34px) 때 버튼 3개 실제
  폭이 128px가 됐는데 grid 컬럼(`.fav-row`)은 92px로 그대로였던 게 원인. 132px로 확장.
- **포트폴리오 "종목 추가" 위치** — "오늘의 Action" 카드 뒤에 따로 떨어져 있던 걸
  보유종목 카드 안으로 이동. 보유 0종목인 신규 사용자도 추가 폼을 봐야 해서 카드
  자체(`#pf-holdings-card`)는 항상 노출, 안쪽 테이블(`#pf-holdings-table-wrap`)만
  `p.available` 여부로 조건부 표시.
- **아이콘 전부 잘려 보이던 근본 원인** — 위 3건을 포함해 화면 전체 아이콘이 "알아볼
  수 없다"는 지적을 받고 재조사, `<svg class="i"><use.../></svg>`에 **`viewBox`가
  아예 없었음**을 발견. 아이콘은 24×24 좌표로 그렸는데 소비하는 `<svg>`는 실제 폭
  13~15px라 좌표를 1:1로 취급 + `overflow:hidden`(SVG UA 기본값) 때문에 왼쪽 위
  15×15만 잘려 보이고 있었다(스프라이트 도입 시점부터 있던 회귀 버그, 그동안
  `getClientRects().length>0`로 "그려지는지"만 확인하고 "잘림 없이 온전한지"는
  검증 안 해서 못 잡았음). `<svg class="i">` 전체(index.html 32곳·app.js 4곳)에
  `viewBox="0 0 24 24"` 일괄 추가로 해결.

### 3.27 오늘의 PICK 로테이션 + 관심종목 모바일 재정렬 (2026-08-21, v88 · js v97)
- **오늘의 AI 투자기회가 며칠째 고정**(실측: 셀트리온) — 사용자 지적. 원인은
  매수/적극매수 tier top5 중 `opportunityScore` 최고 1종목을 매번 그대로 히어로로
  뽑는데, 그 최고점 종목의 펀더멘털이 며칠씩 안 바뀌면 그대로 고정되는 구조였던 것.
  `dailyPickIndex()`(날짜를 시드로 top5 중 하나 선택, 자정 기준 로테이션 — 같은 날
  새로고침해도 안 바뀜)를 추가해 `best = top[dailyPickIndex(top.length)]`로 변경.
  하단 top5 목록은 점수순 그대로 두고 오늘 뽑힌 종목만 `.today-row-picked`로 강조.
- **관심종목 모바일 카드 레이아웃** — 사용자 지적: "즐겨찾기 부분이 이상해". 판단
  배지가 변동내역·메모·태그 아래 맨 밑에 깔리고, 가격은 이름칸이 길어지는 만큼 세로
  중앙에 붕 떠 있던 게 원인. `grid-template-areas`를 "check judge price"/"check name
  price"/"check actions actions"로 바꿔 판단을 이름 바로 위(PC와 같은 순서)로 올리고
  가격은 top-align으로 판단·이름 옆에 붙였다.

---

## 4. 다음 작업 — UX 대개편 (최우선, 사용자 지시 12개 항목) + Phase 3

### 4.0 UX 대개편 (2026-08-14 지시, 2026-08-19 12개 항목 전부 완료 ✅)
사용자가 홈/종목상세/포트폴리오/헤더 UX를 12개 항목으로 구체적으로 지시. **"트래픽 과부하면
차근차근 진행해도 좋다"**고 명시 — 한 번에 다 하지 않고 여러 세션에 걸쳐 순서대로 진행,
2026-08-19에 마지막 ⑦번(탭 구조화, 3.9장)까지 마무리됨. 아래는 각 항목별 완료 기록(참고용).

- ✅ **①번 완료(v41)**: "AI 투자기회 TOP5"가 실시간 랭킹과 뭐가 다른지 모호하다는 지적 →
  `index.html`에 `.today-legend`(종합점수="기업 자체가 좋은 종목" ≠ 투자기회="지금 가격까지
  고려했을 때 좋은 종목") 대비 칩 추가, TOP5 테이블에 **종합점수 컬럼**을 판단 옆에 나란히
  배치(`app.js` today-row 템플릿, `.today-score` 클래스)해서 "점수 높다≠지금 사야 한다"를
  사용자가 직접 비교하도록 함. 섹션 부제도 "지금 사기 좋은 종목"으로 명확화.
- ✅ **②번 완료(v41)**: 홈 최상단(히어로 바로 아래, `today-board`보다 위)에 `#mypf-board` 위젯
  신규 추가(`app.js`의 `loadMyPortfolioWidget()`). 비로그인 시 CTA, 로그인+보유종목 없음 시
  등록 유도, 보유종목 있음 시 `portfolio.py`의 기존 `today_actions`(비중과다/매수타이밍악화/
  추가매수기회/매도신호)를 그대로 재사용해 카드로 표시 — **백엔드 신규 API 없음**, `/api/portfolio`
  재사용. `goHome()`/`loadMe()`/로그인성공/로그아웃 4곳에서 갱신 호출.
- ✅ **③번 완료(v42)**: 종목상세 `#highlight-card`를 리디자인 — "🤖 StockLens AI" 라벨 아래
  판단(🟡분할매수 등)을 42px/900weight로 초대형 표시, 그 바로 아래에 현재가·적정매수가·목표주가·
  상승여력·판단신뢰도 5항목 stat grid(`.hl-grid`, 기존 hl-grid를 `.ai-verdict-box` 안으로 이동),
  그 아래에 "현재 판단" 라벨을 붙인 한줄평(`.verdict-banner`). 기존에 있던 "매수 매력도 XX점"은
  **종합점수**로 라벨 변경 + 대폭 축소(15px, 옅은 색)해서 "판단"보다 확실히 덜 눈에 띄게 — 홈의
  종합점수/투자기회 구분(①번)과 같은 철학. 손절가는 hl-grid에서 제거(이미 가격별 투자전략 하단에
  중복 표시 중이었음).
- ✅ **④번 완료(v42)**: "확신도"를 **"판단 신뢰도"**로 전면 교체(종목상세 hl-grid, 홈 TODAY'S
  PICK, 포트폴리오 AI리밸런싱 안내문 3곳 모두). `%` 접미사도 제거(확률처럼 안 보이게, 그냥
  숫자만). `.info-dot`(ⓘ) 클릭/포커스/호버 시 "실적·밸류·수급·기술적 지표 등 주요 분석 신호의
  일치 정도를 나타냅니다" 툴팁(`.tooltip-pop`, CSS만으로 구현, 신규 컴포넌트라 재사용 가능).
  `analysis.final_verdict()`의 `confidence` 필드명 자체는 안 바꿈(프론트 라벨링 이슈였음).
- ✅ **⑤번 완료(v45)**: 가격별 투자전략의 텍스트 밴드 목록(`.fb-band-row`, 5단계 범위 나열)을
  **가로 눈금선 + 세로 마커** 형태로 교체 — `renderPriceLadder()`(구 `renderFairBuyBands`).
  적극매수/매수/분할매수(fair_buy 3티어) + 현재가(📍, accent색 강조 마커로 다른 점보다 굵고
  높게) + 목표가(🎯)를 실제 가격 비율로 가로선에 배치(균등 간격 아님 — 값 차이가 클수록 멀리
  떨어져 보임, 더 정직한 시각화). 현재가가 밴드 범위 밖(이미 저평가/고평가)이어도 정상 동작하도록
  min/max 기준 동적 스케일링. 모바일에서 포인트 5개가 겹치지 않도록 `.price-ladder`에
  `overflow-x:auto` — 페이지 자체는 가로 스크롤 안 생기고 눈금 영역만 내부 스크롤.
- ✅ **⑥번 완료(v46→v47, 절충안)**: "5초 요약 카드"가 요구한 정보 대부분(가격·판단·점수·신뢰도·
  적정매수가·목표주가·상승여력)은 ③번 리디자인으로 이미 `#highlight-card`에 다 들어가 있었고
  관심/알림/비교 버튼도 바로 위 헤더에 이미 있었음 — **완전 새 카드를 만드는 대신 빠진 조각인
  "부문별 아이콘 6개"만 `#highlight-card`에 추가**(`#hl-cats`, 검증 박스 바로 아래). 홈의
  today-why와 같은 `CATEGORY_ICON` 맵 재사용, 점수 구간(62/42 문턱)에 따라 🟢/🟡/🔴 점을 붙임.
  ⚠️ **v46은 아이콘+색점만 표시하고 부문명은 title 호버 툴팁에만 넣었는데, 사용자가 "차트면
  차트라고 글도 같이 적어줘야지"라고 지적**(모바일에서 호버 자체가 안 먹혀 무슨 부문인지 전혀
  안 보였음) — v47에서 부문명 텍스트를 칩에 항상 같이 표기하도록 수정. **호버/클릭에만 의존하는
  정보 표시는 안 됨, 항상 보이는 텍스트로 넣을 것**(교훈, 판단신뢰도 info-dot 툴팁처럼 "보충
  설명"이 아니라 "이게 뭔지 알려주는 라벨 자체"는 호버 뒤에 숨기면 안 됨).
- ✅ **⑦번 완료(v52, 3.9장 참고)**: 종목상세의 나머지 분석 영역 10개 카드를 7개 탭
  (`[종합][차트][밸류][재무][수급][뉴스·리포트][AI분석]`)으로 묶고 첫 화면은 "종합"만 노출.
  **Phase1에서 의도적으로 범위 제외했던 바로 그 작업** — 실제로 탭 버튼이 통째로 사라지는
  버그와 캔버스 크기 버그를 잡으며 진행했다(상세는 3.9장). Claude AI 심층분석은 "AI 분석" 탭.
- ✅ **⑧번 완료(v44)**: 포트폴리오 화면(`#portfolio-view`) 섹션 순서를 재배치 —
  **요약카드(💼내포트폴리오) → 오늘의Action → 종목추가폼 → 나머지(위험/노출/업종/상관관계/
  리밸런싱/보유종목)**. 요약카드 안에 "AI 포트폴리오 점수" 히어로 블록(`#pf-grade-hero`)을
  신규 추가 — 종목상세 AI 최종판단(③번)과 같은 패턴으로 이모지+점수(`78/100`)+등급을 크게
  표시. 기존 `warnings`(업종·종목 쏠림 경고)에 밸류에이션 양호 시 긍정 신호 한 줄
  (`🟢 종목별 밸류에이션은 양호한 편입니다`, `.pf-warnings li.pos`)을 보태 "경고 목록"이
  아니라 "진단 요약"처럼 보이게 함. **백엔드 변경 없음** — `portfolio.py`가 이미 주는
  grade/score/warnings/today_actions 그대로 재사용, HTML 순서 재배치 + CSS만 추가.
- ✅ **⑨번 완료(v43)**: 로그인/회원가입 모달 상단(탭 바로 아래)에 "로그인하면 무엇이 좋아지나요?"
  박스(`.auth-benefits`) 추가 — 포트폴리오 저장&AI진단/관심종목 저장/가격·매수타이밍 알림/기기간
  동기화. **"AI 투자기록"은 실제로 없는 기능이라 목록에서 제외**(없는 기능을 약속하면 안 됨) —
  대신 실존하는 "포트폴리오 저장 & AI 진단"으로 대체.
- ✅ **⑩번 완료(v43)**: `GET /api/kis/status`가 이제 `public` 필드도 반환(`main.py`, `PUBLIC`
  일 때 `configured`도 강제로 false). 프론트 `refreshKisBtn()`이 페이지 로드 시 즉시 호출돼
  **공개배포에서는 버튼 자체를 아예 숨김**(이전엔 종목 하나를 분석해야만 숨겨졌음 — 그 전까지는
  공개 사용자가 KIS 설정 모달까지 들어갔다가 저장 시점에야 403을 만나는 구멍이 있었음, 이번에
  같이 메꿈). 로컬 실행 시엔 연결 여부에 따라 "⚙ 실시간 시세 연동" ↔ "⚡ 실시간 시세 연결됨"
  라벨만 전환(APP KEY 입력 폼 자체는 원래도 모달 뒤에 있었어서 새로 안 만듦).
- ✅ **⑪번 완료(v43)**: 헤더 "made by 태균" 제거, 대신 페이지 최하단에 `.site-footer`로
  "StockLens © 2026" 추가.
- ✅ **⑫번 완료(v43)**: 히어로 카피 교체 — "분석은 StockLens가. 결정은 당신이." 헤드라인 +
  "흩어진 수백 개의 신호와 뉴스, 증권가의 시선을 하나의 투자 판단으로." 서브카피(사용자가 준
  두 후보를 헤드라인/서브로 결합).

### Phase 3 (알림 강화)

사용자가 제시한 ⑦가격알림 ⑧적정매수가 진입알림 ⑨AI매도신호 ⑩실적·뉴스·수급 급변알림 중
**⑧⑨는 `portfolio_alert.py`, ⑩은 `event_alert.py`로 구현 완료(2026-08-14, 라이브 배포됨).**
실제로 남은 건:
- 기존 알림 조건·문구를 사용자가 그린 목업(`🔔 08:32 삼성전자 적정매수가 진입` 같은 타임스탬프형
  알림 로그)에 맞게 다듬는 작업에 가까울 가능성이 큼
- ⑦ 순수 가격 알림(사용자가 지정한 가격 도달 시)은 아직 없음 — 새 기능

### ⑩ 실적·뉴스·수급 급변 알림 (`app/event_alert.py`, 신규)
관심종목(`watch.py`의 watchlist·push_subs 재사용, 별도 옵트인 UI 없음)을 20분마다 재분석해
**직전 체크 대비 변화(델타)** 를 감지한다 — 절대 수치가 아니라 "새로 바뀐 것"만 알림:
- 📈📉 실적 급변: `metrics.op_growth`(최근 실적 영업이익 YoY)가 갱신되며 ±30% 이상일 때
- 📰 뉴스 급변: 새 기사(URL 기준)가 뜨고 감성이 긍정/부정으로 뚜렷할 때(중립 제외)
- 🔄 수급 급변: 외국인+기관 최근 5일 순매수 합의 부호가 매수↔매도로 전환될 때(국내 전용,
  미국은 `naver.trend()`가 데이터를 안 줘서 flows가 항상 비어 자동 스킵됨)

⚠️ **그 종목의 첫 체크(state 없음)는 기준값만 저장하고 알림을 보내지 않는다** — 안 하면 배포/서버
재시작 직후 기존에 이미 알던 상태 전체가 "새 이벤트"로 오탐돼 스팸이 된다. 종목당 분석은 관심등록자
수와 무관하게 1회만(watchlist를 code로 그룹핑) — 등록자 전원에게는 그 결과만 나눠 보낸다.
쿨다운은 `portfolio_alert.py`와 동일하게 (user, code, signal) 조합 24시간.

착수 전에 사용자에게 "Phase3 이어서 할까요?" 확인 필요 — 직전 대화에서 그렇게 안내해둔 상태.

### 기타 백로그
- `app/dart.py` 미사용 상태 유지 중(밸류에이션 실적방향에 연결하거나, 안 쓸 거면 삭제 검토)
- 탭 구조로 종목상세 페이지 전환(Phase1에서 의도적으로 제외한 항목)
- 미국 종목 원화 병기(환율 환산) — 포트폴리오 해외지원의 전제조건이기도 함

---

## 5. 핵심 설계 결정 & 함정 (건드리기 전에 반드시 읽을 것)

### 5.1 데이터 소스 = 네이버 비공식 API (pykrx 아님)
pykrx는 이 환경에서 KRX 펀더멘털/티커 엔드포인트가 빈 응답 → 쓰지 말 것. 국내:
`m.stock.naver.com/api` · 미국: `api.stock.naver.com` (`naver.is_us(code)`로 자동 라우팅,
6자리 숫자=국내). 미국 코드 형식 불규칙(`AAPL.O`/`JPM`/`ORCL.K`) — 검색 API로 반드시 확인.

### 5.2 값 파싱 함정
네이버는 값에 단위를 붙여 준다(`"23.08배"`, `"12,372원"`, `"1,669조 1,125억"`).
`analysis.to_num()`/`analysis.parse_eok()` 필수 — `float()` 직접 쓰면 전부 None.

### 5.3 밸류에이션 4대 함정 (`app/valuation.py`, 전부 실측으로 확인)
1. 선행PER에 **컨센서스 EPS를 섞지 않는다** — 다음 해가 실적인 경우만 계산.
2. **PEG 성장률 상한 50%** — 저기반 회복(적자→흑자 등)을 그대로 나누면 PEG 0.01처럼 무의미해짐.
3. **동종업계는 중앙값** + PER 200배 초과 제외 — 이상치 1종목이 업종평균을 통째로 오염시킴.
4. **미국 단위 환산**: 시총=억 USD, 재무제표=백만 USD. EBITDA는 100으로 나눠야 함.
5. `fair_buy_price()`는 각 관점의 괴리배수를 **현재가 0.5~1.6배로 clamp** — 안 하면 PEG 환산식이
   폭주해 적정가가 현재가의 3배 이상으로 나올 수 있음(실제 삼성전자에서 발견).

### 5.4 점수화 편향 재도입 금지 (`app/analysis.py`)
- PEG 보정에 성장률을 그대로 곱하지 않는다(저기반 폭증 방지, 상한 40~50%·중앙값 사용)
- 부채비율 >500%는 금융업 구조로 보고 중립 처리(은행·지주를 재무위험으로 깎지 않음)
- 점수 로직 변경 시 **반드시 섹터가 다른 종목 묶음으로 재검증**(바이오·금융·자동차·통신 등)

### 5.5 AI 리밸런싱 동적 상한 (`app/portfolio.py`, 5.3장 위 참고)
고정 30% 상한은 보유종목이 적을 때 모순을 만든다 — `max(30%, 100%/종목수×1.4)`로 동적 완화 필수.

### 5.6 랭킹 백그라운드 집계 & 서버 부하
- 국내 즉시 시작 / 미국 90초 지연 시작, 30분마다 재계산, 20종목마다 부분결과 공개(`computing:true`
  폴링). **배포(재시작) 직후 몇 분간 `/api/analyze`가 19~30초까지 느려질 수 있음** — 랭킹 재계산이
  네트워크를 다 써서 생기는 경합, 코드 버그 아님. "느리다" 보고를 받으면 먼저
  `/api/ranking?market=US`의 `computing` 값부터 확인할 것.
- 새 무거운 집계는 **랭킹의 `_score()` 배경 루프에 절대 추가하지 말 것**(180+190종목마다 비용이
  곱해짐) — 온디맨드 단일 종목 추가호출(홈 Today's Pick 방식) 또는 캐시 재사용만 쓸 것.

### 5.7 공개 배포 모드 (`STOCKLENS_PUBLIC=1`)
KIS 키 저장·AI 리포트 403 차단, IP별 레이트리밋 활성. 로컬 실행 시엔 이 값이 없어 전 기능 사용 가능.

---

## 6. 실행 / 배포 방법

### 로컬 실행
```bash
cd C:\Users\권태균\stocklens
python -m uvicorn main:app --port 8899
```
→ http://127.0.0.1:8899 · **`--reload` 없음 → 백엔드(.py) 수정 시 서버 재시작 필수.**

### 🚨 CSS/JS 수정 시 반드시 캐시 버전 올리기
`static/index.html`의 `style.css?v=N` / `app.js?v=N`을 **둘 다 +1**. 현재 style.css v88 · app.js v97(둘이 항상 같이 가진 않음 — 각자 독립적으로 +1).

### 오라클 배포 (Render 자동배포 없음 — 수동 배포 필수)
SSH 키: `C:\Users\권태균\Downloads\ssh-key-2026-08-06.key` (계정 `ubuntu@161.33.201.126`).
**매번 권한 재설정 필요**(Windows가 리셋함):
```powershell
icacls $key /inheritance:r
icacls $key /grant:r "$($env:USERNAME):(R)"
```
⚠️ **ssh/scp는 반드시 PowerShell 도구로 실행할 것** — Bash 도구는 auto-mode 분류기가 원격 접속을
위험 작업으로 보고 차단한다.

변경된 파일만 개별 scp(전체 tar 불필요, 더 빠르고 안전):
```powershell
$key = "C:\Users\권태균\Downloads\ssh-key-2026-08-06.key"
scp -i $key -o StrictHostKeyChecking=no "C:\Users\권태균\stocklens\<파일>" "ubuntu@161.33.201.126:/tmp/<파일명>"
ssh -i $key -o StrictHostKeyChecking=no ubuntu@161.33.201.126 @'
sudo cp /tmp/<파일명> /opt/stocklens/<원래경로>/<파일명>
sudo chown stocklens:stocklens /opt/stocklens/<원래경로>/<파일명>
sudo systemctl restart stocklens
sudo systemctl is-active stocklens
'@
```
라이브 확인: `curl -s "https://stocklens.161-33-201-126.nip.io/" | grep -o 'style.css?v=[0-9]*'`

GitHub push도 계속 병행(백업 목적):
```bash
git add -A && git commit -m "..." && git push origin main
```

### 프론트엔드 검증
- **스크린샷 도구는 계속 타임아웃됨**(외부 CDN 로드 대기) → `javascript_tool`로 DOM 직접 eval 검증할 것.
- 종목 분석 렌더는 **8~9초** 대기 후 확인.
- PowerShell은 **한글 키 접근 불가**(`$a.total.categories.'가치평가'` → 빈값) — 부문점수 확인은 Python으로.
- PowerShell `Invoke-RestMethod -Body`는 한글을 콘솔 코드페이지 때문에 `?`로 깨뜨림 — 실제 버그인지
  헷갈리면 브라우저 fetch로 재현할 것(인코딩 문제가 가짜 단서였던 사례 있음).

### 삭제/정리 작업 시 원칙
DELETE API 호출 후 응답 `{"ok":true}`만 믿지 말고 **반드시 후속 GET으로 실제 상태 재확인**할 것
(과거 이렇게 안 해서 정리 누락이 뒤늦게 발견된 사례 있음).

---

## 7. 알려진 제약 / 미해결

| 항목 | 내용 |
|---|---|
| 포트폴리오 미국종목 환율 | 실시간 환산 지원됨(2026-08-18, 3.6장) — 다만 과거 시계열(변동성·상관관계)은 현재 환율을 1년치에 균일 적용하는 근사치, 정밀 백테스트용은 아님 |
| 포트폴리오 ETF 룩스루 | 데이터 소스 없어 의도적으로 미지원(QQQ 내부 NVDA 비중 역산 등) |
| 웹푸시 실제 수신 | 자동화 브라우저는 Notification 권한이 항상 거부 상태라 백엔드 로직만 단위테스트로 검증됨 — 실사용 여부는 사용자가 실기기에서 확인 필요 |
| 미국 ROE·부채비율 | 네이버 미제공 → "-" 표시, 안정성은 프록시로 대체 |
| 과거 PER 이력 | 네이버가 실적 3년만 제공 → 실제 사용 연수를 정직하게 표기 |
| EV/EBITDA | 미국 전용(국내 EBITDA 미제공) + 순차입금 미차감 근사 |
| 실적발표 예정일 | 네이버 API에 필드 자체가 없음(확인됨) — 관련 기능은 사용자 요청으로 전부 제거됨(2026-08-14) |
| 데이터 출처 | 네이버 비공식 API. 스펙 변경 시 파싱 깨질 수 있음(과거 실제 발생) |
